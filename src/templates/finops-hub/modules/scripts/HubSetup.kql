// Grant ADF identity access to the cluster
.add database Hub admins ('aadapp=$$adfPrincipalId$$;$$adfTenantId$$')

.create-or-alter function
with (docstring = 'Gets all cost and usage records with the latest supported version of the FOCUS schema.', folder = 'Ingestion')
Costs()
{
    database('Ingestion').Costs_v1_0
}


.create-or-alter function
with (docstring = 'Gets all prices with the latest supported version of the FOCUS schema.', folder = 'Ingestion')
Prices()
{
    database('Ingestion').Prices_v1_0
}