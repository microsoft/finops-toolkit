---
title: FinOps toolkit SRE Agent release training deck outline V8
permalink: finops-toolkit-sre-agent-release-training-deck-outline-v8
type: memory
status: all-clusters-populated
tags:
  - sre-agent
  - training
  - deck
  - field-enablement
  - v8
  - mcaps-asks
  - L400
created: '2026-05-02T23:36:00.0000000+00:00'
modified: '2026-05-02T23:36:00.0000000+00:00'
---

<!-- DECK-LABELS
cover.built-by-prefix: Built by
cover.partnership-prefix: In partnership with
cover.subtitle-prefix: Subtitle
cover.audience-prefix: Audience tag
cover.cobrand-prefix: Co-brand
ask.footer-prefix: Asks
ask.footer-separator: , #
ask.footer-first-mark: '#'
ask-b.owners-header: Who owns this
ask-c.monday-header: Monday move
ask-c.code-marker-prefix: srectl
index.entry-separator: ' · '
index.ask-mark: '#'
pptx.author: FinOps Toolkit community
-->

# [[FinOps toolkit SRE Agent]] release training deck outline (V8)

> **Audience.** [[MCAPS]] worldwide — AE / SE / CSA / CSAM / partner architects. Includes the existing [[FinOps Toolkit]] community in MCAPS. **L300/L400 density.** Not a cold-pitch room.

> **Canonical ask source.** `memory://work/mcaps/mcaps-internal-asks-finops-toolkit-azure-capacity-ai-cost-may-2026`. **Every body slide cites at least one ask number** in its layout directive (`addresses=N,N`) AND in its speaker notes (full memory URI + verbatim ask reference). The 73-ask table in that memory note is the single source of truth — do not re-derive.

> **Structure.** Two parts × ~45 min, each ~30-40 slides. Each ask cluster = 3 slides (A/B/C = **ASK / SHOW / TELL**). **A = ASK** (verbatim customer quote, low density, big type). **B = SHOW** (the answer — named tools/tasks + chart from a real run, eye candy carries the slide). **C = TELL** (the L400 details — YAML, cron, KQL, runtime, Monday move; code carries the slide). Plus Part 3 wrap (~8 slides) for honest-not-solved cluster + close.

> **Build mechanics.** New layout kinds: `ASK_A`, `ASK_B`, `ASK_C` + per-row directive `addresses=N,N verdict=green|yellow|red` parsed by build.py. Renderer emits ask-coverage footer chip on every body slide. Speaker-notes citation format below.

> **Speaker notes citation format (mandatory on every body slide).**
> ```
> Sources: MCAPS ask #N — "<verbatim ask theme from canonical table row>"
> Canonical: memory://work/mcaps/mcaps-internal-asks-finops-toolkit-azure-capacity-ai-cost-may-2026
> Coverage verdict per canonical: 🟢 ships-today | 🟡 partial | 🔴 out-of-scope
> Evidence pack: src/templates/sre-agent/training/release-deck/evidence/tasks/<task>.md
> Chart proof: src/templates/sre-agent/training/release-deck/charts/svg/<chart>.svg
> ```
> The build script extracts the citation block to render the footer chip; the rest stays in speaker notes for the field to follow back to source if needed.

---

## Plan summary

| Part | Theme | Cluster count | Slide count (incl. chrome) | Time |
|---|---|---|---|---|
| 1 | Deal motion (sells / defends / expands) | 11 | ~38 | 45 min |
| 2 | Operate motion (run-the-shop) | 15 | ~50 | 45 min |
| 3 | Honest "what we don't solve" + close | 5 honest + 3 close | ~8 | 5-10 min wrap |
| **Total** | | **31 clusters** | **~96 slides** | **2 × 45-min slots** |

---

## Worked example — Cluster P1.1: Quota ≠ Capacity (citation format reference)

The first cluster is fully built as the format reference for the citation block and layout directive shape. All 31 clusters are now populated following this pattern. Every speaker-notes cell ends with the citation block.

### Table 1 — Part 1: Deal motion (~38 slides)

| # | Cluster | Asks (verbatim) | Title | Content | Speaker notes | Layout | Screenshots / assets |
|---|---|---|---|---|---|---|---|
| 0.1 | Intro | — | FinOps toolkit SRE Agent — Part 1 of 2 | • Subtitle: Manage your Azure estate — capacity and cost — through the conversations your customers are actually having.<br/>• Audience tag: FinOps Toolkit community in MCAPS, worldwide.<br/>• Co-brand: FinOps Toolkit community + aka.ms/finops/toolkit | The questions stalling your deals are repeatable. Quota that says yes but the deployment fails. Reservation bills nobody can reconcile. AI capacity requests denied without a clear reason. Discounts the CFO can't trust. Tagging that broke after the last acquisition.<br/><br/>The FinOps Toolkit SRE Agent answers most of these on a schedule, with evidence, in Teams.<br/><br/>Part one walks the questions you hear before signature. Part two covers the operating rhythm after. Part three names what this release does not own. | kind=TITLE | None |
| 0.2 | Intro | — | Part 1 — deal motion | • P1.1 · green · Quota ≠ Capacity · 8,13,14,23,24<br/>• P1.2 · green · CRG mechanics + idle billing waste · 9,12,19<br/>• P1.3 · yellow · RI vs SP vs ODCR + sequencing · 17,18<br/>• P1.4 · green · SGS capacity-blocked milestones · 15<br/>• P1.5 · yellow · Customer refuses to change region · 20<br/>• P1.6 · yellow · AOAI quota + GPU + token forecasting · 1,2,7<br/>• P1.7 · red · AOAI throttling/region/GA · 3,4,5,6<br/>• P1.8 · green · MACC discount verification · 35<br/>• P1.9 · green · Vendor displacement · 47<br/>• P1.10 · yellow · Sovereign / Gov cloud · 60,72<br/>• P1.11 · yellow · EA → MCA migration safety · 52 | These are the questions you hear from customers before signature.<br/><br/>The dot tells you what to say. Green: a complete answer with evidence. Yellow: evidence and a clear boundary. Red: a different owner. We show the routing path later.<br/><br/>The most common ones are at the top. Quota that says yes but the deployment fails. Reservation bills nobody can reconcile. AI capacity requests denied without a clear reason. | kind=INDEX | None |
| 0.3 | Intro | — | Part 2 — operate motion | • P2.1 · green · Capacity rhythm · 21,24<br/>• P2.2 · yellow · AOAI token attribution · 25,26,28<br/>• P2.3 · yellow · AI budget brake pedal · 27<br/>• P2.4 · yellow · AI model substitution + prompt economics · 29,30<br/>• P2.5 · green · Daily cost-mover briefing · 36,37<br/>• P2.6 · green · Anomaly detection chain · 41<br/>• P2.7 · yellow · CFO reconciliation trust gap · 32,33,34<br/>• P2.8 · green · Hub freshness verdicts · 38,58,66 |  After signature, the customer judges the toolkit by what runs every week.<br/><br/>These are the operating clusters. Same dot system. Green ships a complete answer. Yellow gives evidence with a boundary. Red routes to a different owner.<br/><br/>The first eight cover capacity rhythm, AI cost attribution, budget controls, model substitution, daily movers, anomaly readiness, CFO reconciliation, and hub data freshness.  | kind=INDEX | None |
| 0.4 | Intro | — | Part 2 + 3 — close-out | • P2.9 · yellow · RI/SP math + portal reconciliation · 62,63<br/>• P2.10 · yellow · Allocation hygiene + post-M&A · 36,38<br/>• P2.11 · green · FinOps Framework operationalization · 43,45<br/>• P2.12 · green · Dashboard adoption → Teams cadence · 70<br/>• P2.13 · green · Native CM gaps (what we exceed) · 48<br/>• P2.14 · green · Run-it-for-them ops model · 55<br/>• P2.15 · red · FTK supportability — HONEST · 49,50,51 |  These are the last seven operating clusters.<br/><br/>Reservation reconciliation. Allocation hygiene. Framework operationalization. Teams cadence. Native Cost Management gaps. The run-it-for-them ops model. Supportability.<br/><br/>Supportability sits at the end on purpose. Once the customer has seen the operating value, the conversation about what is supported lands without breaking trust.  | kind=INDEX | None |
| **P1.1.A** | **Quota ≠ Capacity** | #8 "Quota ≠ Capacity — I have quota, why deploy fails?" · #13 "Zonal capacity / AZ stockouts (East US, North Europe)" · #14 "SKU availability / offer restrictions" · #23 "CI/CD + capacity pre-flight checks" · #24 "Proactive capacity monitoring (no real-time API)" | **Ask** | • > "I have quota in this subscription, why is the deployment failing?"<br/>• > "Portal says I have vCPUs available. The deploy still fails with SkuNotAvailable."<br/>• > "The customer finds out at deploy time." | This is the ticket the agent is built to prevent.<br/><br/>Quota is only one signal. SKU availability, reservation group posture, non-compute limits, and hub freshness can all create the failure pattern before quota looks exhausted.<br/><br/>The scheduled monitor brings those signals together before the deployment attempt becomes an escalation. | kind=ASK_A · addresses=8,13,14,23,24 verdict=green | None |
| **P1.1.B** | **Quota ≠ Capacity** | #8 "Quota ≠ Capacity — I have quota, why deploy fails?" · #13 "Zonal capacity / AZ stockouts (East US, North Europe)" · #14 "SKU availability / offer restrictions" · #23 "CI/CD + capacity pre-flight checks" · #24 "Proactive capacity monitoring (no real-time API)" | **Implementation** | • `CapacityDailyMonitor` is the preventive check for this cluster.<br/>• Verified callable tools: `vm-quota-usage`, `non-compute-quotas`, `sku-availability`, `capacity-reservation-groups`, and `data-freshness-check`.<br/>• The task reads quota, SKU restrictions, reservation group allocation, non-compute limits, and hub freshness in one run.<br/>• The output classifies the risk surface before the customer has to discover it through a failed deployment.<br/>• Teams delivery depends on the notification connector and tool assignment being configured after deployment. | The failure pattern is late discovery. The inversion is a scheduled capacity board before the deploy.<br/><br/>The daily monitor does not pretend Azure exposes one real-time capacity answer. It assembles the signals the field can verify: quota, SKU restriction state, reservation group allocation, non-compute limits, and hub freshness.<br/><br/>Grounding: memory://thinking/sequential/2026/05/03/session-1777846402443-12272 and `capacity-daily-monitor.yaml`. | kind=ASK_B · addresses=8,13,14,23,24 verdict=green | None |
| **P1.1.C** | **Quota ≠ Capacity** | #8 "Quota ≠ Capacity — I have quota, why deploy fails?" · #13 "Zonal capacity / AZ stockouts (East US, North Europe)" · #14 "SKU availability / offer restrictions" · #23 "CI/CD + capacity pre-flight checks" · #24 "Proactive capacity monitoring (no real-time API)" | **Details** | • Scheduled task: `sre-config/scheduled-tasks/capacity-daily-monitor.yaml`.<br/>• Owner agent: `azure-capacity-manager`.<br/>• Verified tool surface:<br/>```<br/>vm-quota-usage<br/>non-compute-quotas<br/>sku-availability<br/>capacity-reservation-groups<br/>data-freshness-check<br/>```<br/>• Decision pattern:<br/>```<br/>quota pressure → quota or demand-planning action<br/>SKU restriction → SKU, offer, or regional availability action<br/>CRG mismatch → reservation group allocation review<br/>hub stale → fix data trust before finance or capacity review<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke capacity-daily-monitor`. | This slide is intentionally mechanical.<br/><br/>One scheduled task. One owner agent. Five verified tools. The agent does not need a fictional capacity API to prevent the ticket; it needs enough real signals to tell the field which surface is failing before the customer opens a case.<br/><br/>Grounding: `00-tool-task-agent-truth.md` and `capacity-daily-monitor.yaml`. | kind=ASK_C · addresses=8,13,14,23,24 verdict=green | None |
| **P1.2.A** | **CRG mechanics + idle billing waste** | #9 "CRG mechanics + idle billing cost shock" · #12 "CRG cross-subscription sharing (preview)" · #19 "RI/SP + CRG double-billing risk" | **Ask** | • > "CRG mechanics + idle billing cost shock."<br/>• > "CRG cross-subscription sharing preview."<br/>• > "RI/SP + CRG double-billing risk." | Reservation groups guarantee compute supply. Reservations and savings plans cut the price.<br/><br/>Customers conflate the two. They end up either paying for idle reserved capacity or assuming a discount they did not buy.<br/><br/>This is where you separate supply control from price control before the cost conversation starts. | kind=ASK_A · addresses=9,12,19 verdict=green | None |
| **P1.2.B** | **CRG mechanics + idle billing waste** | #9 "CRG mechanics + idle billing cost shock" · #12 "CRG cross-subscription sharing (preview)" · #19 "RI/SP + CRG double-billing risk" | **Implementation** | • `CapacityMonthlyPlanning` prevents CRG cost shock from staying invisible until invoice review.<br/>• Verified callable tools: `capacity-reservation-groups`, `vm-quota-usage`, `commitment-discount-utilization`, `cost-forecasting-model`, and `savings-summary-report`.<br/>• The task separates reserved supply from commercial discount coverage so idle reserved capacity is not confused with a reservation or savings-plan decision.<br/>• The useful answer is not "buy more"; it is whether the risk is supply, utilization, forecast, or commitment coverage.<br/>• Cross-subscription CRG sharing remains a preview-boundary discussion. | CRG tickets usually start because the customer learned about idle reserved supply from the bill.<br/><br/>The monthly planning task checks the reservation group posture, current quota context, commitment utilization, forecast pressure, and savings summary before that invoice review.<br/><br/>Grounding: `capacity-monthly-planning.yaml` and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=9,12,19 verdict=green | None |
| **P1.2.C** | **CRG mechanics + idle billing waste** | #9 "CRG mechanics + idle billing cost shock" · #12 "CRG cross-subscription sharing (preview)" · #19 "RI/SP + CRG double-billing risk" | **Details** | • Scheduled task: `sre-config/scheduled-tasks/capacity-monthly-planning.yaml`.<br/>• Owner agent: `azure-capacity-manager`.<br/>• Verified tool surface:<br/>```<br/>capacity-reservation-groups<br/>vm-quota-usage<br/>commitment-discount-utilization<br/>cost-forecasting-model<br/>savings-summary-report<br/>```<br/>• Decision pattern:<br/>```<br/>no CRGs → focus on commitment coverage<br/>low CRG utilization → review reserved supply<br/>forecast growth → plan capacity and commitment together<br/>preview sharing request → handoff with a preview boundary<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke capacity-monthly-planning`. | The mechanism keeps supply and price separate.<br/><br/>Capacity reservation groups answer whether supply is held. Commitment utilization and savings summary answer whether the price path is working. The forecast tells the team whether the current posture survives the next demand step.<br/><br/>No unsupported recommendation tool is claimed here. | kind=ASK_C · addresses=9,12,19 verdict=green | None |
| **P1.3.A** | **RI vs SP vs ODCR + sequencing** | #17 "RI vs SP vs ODCR positioning" · #18 "Commitment + capacity sequencing" | **Ask** | • > "RI vs SP vs ODCR positioning."<br/>• > "Commitment + capacity sequencing."<br/>• > "What do you buy first, and how do you avoid over-committing?" | What do you buy first?<br/><br/>The wrong sequence strands commitment dollars or buys discounts on supply that does not exist. Stable workloads validate reservations. Flexible compute runs through savings plans. Supply-blocked workloads need a separate motion entirely.<br/><br/>The agent ranks the math. You sequence the conversation. | kind=ASK_A · addresses=17,18 verdict=yellow | None |
| **P1.3.B** | **RI vs SP vs ODCR + sequencing** | #17 "RI vs SP vs ODCR positioning" · #18 "Commitment + capacity sequencing" | **Implementation** | • `CapacityQuarterlyStrategy` is the safe release surface for sequencing supply and discount conversations.<br/>• Verified callable tools: `vm-quota-usage`, `reservation-recommendation-breakdown`, `commitment-discount-utilization`, and `savings-summary-report`.<br/>• The task supports the sequence: confirm capacity posture, check commitment utilization, review reservation recommendations, then summarize savings exposure.<br/>• ODCR commercial terms and approval paths remain outside the agent.<br/>• This is a yellow answer because the agent informs the sequence; it does not decide the purchase or the capacity program. | The failure pattern is buying a discount before the supply problem is understood.<br/><br/>The quarterly strategy task keeps the order clean. Capacity posture first. Commitment utilization second. Reservation recommendation breakdown third. Savings exposure last.<br/><br/>Grounding: `capacity-quarterly-strategy.yaml` and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=17,18 verdict=yellow | None |
| **P1.3.C** | **RI vs SP vs ODCR + sequencing** | #17 "RI vs SP vs ODCR positioning" · #18 "Commitment + capacity sequencing" | **Details** | • Scheduled task: `sre-config/scheduled-tasks/capacity-quarterly-strategy.yaml`.<br/>• Owner agent: `azure-capacity-manager`.<br/>• Verified tool surface:<br/>```<br/>vm-quota-usage<br/>reservation-recommendation-breakdown<br/>commitment-discount-utilization<br/>savings-summary-report<br/>```<br/>• Decision pattern:<br/>```<br/>capacity pressure → solve supply before discount action<br/>stable utilization → validate reservation candidate<br/>flexible demand → compare savings-plan exposure<br/>ODCR request → handoff to the Azure capacity owner path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke capacity-quarterly-strategy`. | The slide names the boundary instead of hiding it.<br/><br/>The agent can assemble the signals that keep the sequence honest. It cannot approve an ODCR path, make a commercial purchase, or guarantee supply.<br/><br/>That honesty prevents a second ticket after finance acts on a half-answer. | kind=ASK_C · addresses=17,18 verdict=yellow | None |
| **P1.4.A** | **SGS capacity-blocked milestones** | #15 "SGS capacity-blocked milestones (MACC risk)" | **Ask** | • > "SGS capacity-blocked milestones."<br/>• > "Which milestone is blocked, and is MACC now at risk?" | When a milestone slips, the deal team needs to know whether capacity is the actual blocker or whether the next dependency lives somewhere else.<br/><br/>The quarterly strategy gives them that triage answer with evidence.<br/><br/>Green capacity moves the conversation forward. Yellow or red triggers a remediation plan. | kind=ASK_A · addresses=15 verdict=green | None |
| **P1.4.B** | **SGS capacity-blocked milestones** | #15 "SGS capacity-blocked milestones (MACC risk)" | **Implementation** | • `CapacityQuarterlyStrategy` gives the capacity context for milestone risk without claiming ownership of SGS or MACC process state.<br/>• Verified callable tools: `vm-quota-usage`, `reservation-recommendation-breakdown`, `commitment-discount-utilization`, and `savings-summary-report`.<br/>• The task can show whether quota pressure or commitment exposure is a plausible blocker before the account team escalates the milestone.<br/>• SGS milestone ownership, MACC attainment, and commercial recovery stay with the account and capacity process owners.<br/>• The useful output is a bounded capacity brief, not a promise that the milestone is recovered. | The ticket-prevention value here is triage.<br/><br/>The quarterly task can tell the team whether quota and commitment signals point to a capacity risk. It cannot inspect or repair the SGS milestone process itself.<br/><br/>Grounding: `capacity-quarterly-strategy.yaml` and WorkIQ conversation `3d36e6e4-1a0c-486e-b5cf-692cdfd9bd04`. | kind=ASK_B · addresses=15 verdict=green | None |
| **P1.4.C** | **SGS capacity-blocked milestones** | #15 "SGS capacity-blocked milestones (MACC risk)" | **Details** | • Scheduled task: `sre-config/scheduled-tasks/capacity-quarterly-strategy.yaml`.<br/>• Owner agent: `azure-capacity-manager`.<br/>• Verified tool surface:<br/>```<br/>vm-quota-usage<br/>reservation-recommendation-breakdown<br/>commitment-discount-utilization<br/>savings-summary-report<br/>```<br/>• Decision pattern:<br/>```<br/>quota pressure present → capacity evidence for the process owner<br/>commitment exposure present → finance and capacity review<br/>no capacity signal → milestone blocker likely lives elsewhere<br/>commercial attainment question → account-team commercial path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke capacity-quarterly-strategy`. | The point is not to make the agent an SGS system.<br/><br/>The point is to prevent an unsupported ticket by showing whether capacity evidence exists before the escalation starts. If the signals are clean, the blocker likely lives in process, governance, or commercial ownership.<br/><br/>That boundary is part of the answer. | kind=ASK_C · addresses=15 verdict=green | None |
| **P1.5.A** | **Customer refuses to change region** | #20 "Customer refuses to change region" | **Ask** | • > "The customer refuses to change region."<br/>• > "What do you do when the required SKU is blocked where they insist on running?" | Sometimes the customer will not move regions.<br/><br/>The required SKU might still be available — or it might be an offer-eligibility issue rather than a capacity one. Either way, you need evidence before you ask the customer to relocate the workload.<br/><br/>That evidence is what protects the deal. | kind=ASK_A · addresses=20 verdict=yellow | None |
| **P1.5.B** | **Customer refuses to change region** | #20 "Customer refuses to change region" | **Implementation** | • `SkuAvailabilityAudit` prevents a region argument from starting with the wrong diagnosis.<br/>• Verified callable tool: `sku-availability`.<br/>• The task identifies whether the requested SKU is allowed for the subscription and region before anyone asks the customer to relocate a workload.<br/>• Adjacent quota and non-compute checks live in other capacity tasks; do not imply this audit owns every blocker.<br/>• This is yellow because service-specific availability and customer region policy still need the appropriate platform owner. | The failure pattern is blaming capacity when the issue may be SKU eligibility or a service-specific constraint.<br/><br/>The SKU audit gives one clean signal: whether the requested SKU is available in the requested region for that subscription context. That answer prevents a premature region negotiation.<br/><br/>Grounding: `sku-availability-audit.yaml` and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=20 verdict=yellow | None |
| **P1.5.C** | **Customer refuses to change region** | #20 "Customer refuses to change region" | **Details** | • Scheduled task: `sre-config/scheduled-tasks/sku-availability-audit.yaml`.<br/>• Owner agent: `azure-capacity-manager`.<br/>• Verified tool surface:<br/>```<br/>sku-availability<br/>```<br/>• Decision pattern:<br/>```<br/>SKU available → region objection is not explained by this signal<br/>SKU unavailable → evaluate SKU family, offer, and service-owner path<br/>quota concern remains → use the capacity monitor or quota audit<br/>private or sovereign constraint → validate with the platform owner<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke sku-availability-audit`. | This is deliberately narrow.<br/><br/>The SKU audit answers the availability question. If quota, non-compute limits, private networking, or sovereign constraints are the concern, bring in the matching owner or scheduled task instead of stretching this one beyond its verified tool surface.<br/><br/>The narrowness is why the answer is credible. | kind=ASK_C · addresses=20 verdict=yellow | None |
| **P1.6.A** | **AOAI quota + GPU + token forecasting** | #1 "AOAI quota vs GPU hardware capacity — denied requests" · #2 "AOAI PayGo → PTU transition timing + sizing" · #7 "Forecasting AI / token-based workloads (non-VM)" | **Ask** | • > "AOAI quota vs GPU hardware capacity — why was the request denied?"<br/>• > "AOAI PayGo to PTU transition timing + sizing."<br/>• > "Forecasting AI or token-based workloads." | AI deployments hit denials all the time.<br/><br/>Was it AOAI quota? GPU hardware? PTU sizing? Customers cannot tell, and the answer determines who fixes it.<br/><br/>The agent gives you cost shape, model mix, and attribution. PTU sizing and GPU capacity stay with Azure AI Foundry. That handoff is the slide. | kind=ASK_A · addresses=1,2,7 verdict=yellow | None |
| **P1.6.B** | **AOAI quota + GPU + token forecasting** | #1 "AOAI quota vs GPU hardware capacity — denied requests" · #2 "AOAI PayGo → PTU transition timing + sizing" · #7 "Forecasting AI / token-based workloads (non-VM)" | **Implementation** | • `AIWorkloadCostAnalysis` prevents the wrong AI capacity question by separating cost evidence from platform-capacity ownership.<br/>• Verified callable tools: `ai-token-usage-breakdown`, `ai-model-cost-comparison`, `ai-daily-trend`, and `ai-cost-by-application`.<br/>• The task shows token volume, model mix, daily cost trend, and application attribution.<br/>• It informs PayGo, PTU, and forecast conversations; it does not diagnose GPU hardware scarcity or live AOAI service capacity.<br/>• This is yellow because Azure AI Foundry owns PTU sizing, region/model availability, and service-capacity answers. | The failure pattern is treating an AOAI denial as if cost telemetry can explain every platform constraint.<br/><br/>The AI workload task gives the field the evidence it really owns: token volume, model mix, daily cost trend, and application attribution. That prevents a vague ticket and creates a clean Foundry handoff when capacity is the issue.<br/><br/>Grounding: `ai-workload-cost-analysis.yaml` and WorkIQ conversation `3d36e6e4-1a0c-486e-b5cf-692cdfd9bd04`. | kind=ASK_B · addresses=1,2,7 verdict=yellow | None |
| **P1.6.C** | **AOAI quota + GPU + token forecasting** | #1 "AOAI quota vs GPU hardware capacity — denied requests" · #2 "AOAI PayGo → PTU transition timing + sizing" · #7 "Forecasting AI / token-based workloads (non-VM)" | **Details** | • Scheduled task: `sre-config/scheduled-tasks/ai-workload-cost-analysis.yaml`.<br/>• Owner agent: `chief-financial-officer`.<br/>• Verified tool surface:<br/>```<br/>ai-token-usage-breakdown<br/>ai-model-cost-comparison<br/>ai-daily-trend<br/>ai-cost-by-application<br/>```<br/>• Decision pattern:<br/>```<br/>token cost rising → review model mix and prompt economics<br/>application attribution missing → fix workload ownership before commitment<br/>PTU sizing question → Azure AI Foundry owner path<br/>GPU hardware or model availability question → Azure AI Foundry owner path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke ai-workload-cost-analysis`. | The mechanics keep cost analysis from becoming a fake capacity answer.<br/><br/>The CFO agent owns the AI spend view. Foundry owns platform capacity, throttling behavior, and model availability. The clean split prevents the support ticket from starting in the wrong queue.<br/><br/>No extra platform-inventory or runtime claim is needed. | kind=ASK_C · addresses=1,2,7 verdict=yellow | None |
| **P1.7.A** | **AOAI throttling/region/GA** | #3 "AOAI PTU throttling confusion (TPM still applies)" · #4 "AOAI model availability by region" · #5 "AOAI production capacity GA expectations" · #6 "AOAI TPM/RPM tier behavior" | **Ask** | • > "AOAI PTU throttling confusion."<br/>• > "AOAI model availability by region."<br/>• > "AOAI production capacity GA expectations."<br/>• > "AOAI TPM/RPM tier behavior." | PTU throttling, model availability by region, GA timing, and tier behavior are owned by the Azure AI Foundry product group.<br/><br/>The agent does not have those signals. Saying so out loud earns trust before the customer asks the wrong question.<br/><br/>This is the honest red boundary. | kind=ASK_A · addresses=3,4,5,6 verdict=red | None |
| **P1.7.B** | **AOAI throttling/region/GA** | #3 "AOAI PTU throttling confusion (TPM still applies)" · #4 "AOAI model availability by region" · #5 "AOAI production capacity GA expectations" · #6 "AOAI TPM/RPM tier behavior" | **Implementation** | • This agent gives token cost, model mix, and Azure resource inventory. It does not give live AOAI throttling telemetry, model-by-region GA inventory, or GA roadmap commitments.<br/>• Azure AI Foundry PG owns model availability and throttling metrics. The Azure AI capacity team owns production-capacity GA timing.<br/>• Your answer is the boundary statement, not a fake chart. | The agent provides token cost, model mix, and resource inventory — useful adjacent context.<br/><br/>It does not provide live throttling telemetry, regional model GA inventory, or GA roadmap commitments.<br/><br/>The boundary is the slide. Use it. Do not overclaim. | kind=ASK_B · addresses=3,4,5,6 verdict=red | None |
| **P1.7.C** | **AOAI throttling/region/GA** | #3 "AOAI PTU throttling confusion (TPM still applies)" · #4 "AOAI model availability by region" · #5 "AOAI production capacity GA expectations" · #6 "AOAI TPM/RPM tier behavior" | **Details** | • Boundary owner: Azure AI Foundry PG for model availability and throttling metrics; Azure AI capacity team for GA roadmap and service-capacity timing.<br/>• Do not claim live PTU throttle diagnosis, region-by-model GA guarantees, or TPM/RPM semantics from this agent or this deck.<br/>• Evidence package for the owner path:<br/>```<br/>tenant<br/>region<br/>model<br/>deployment type<br/>observed timestamp<br/>observed service behavior<br/>AI workload cost and token context when available<br/>``` | When a customer hits AOAI throttling, the agent can contribute adjacent evidence: token cost, model mix, and application attribution.<br/><br/>It still does not own Foundry platform behavior. Send behavior and capacity timing questions to the Foundry and Azure AI capacity owners with the evidence package attached.<br/><br/>That boundary prevents a wrong support path. | kind=ASK_C · addresses=3,4,5,6 verdict=red | None |
| P1.8.A | P1.8 MACC discount verification | #35 "CFO: MACC discount verification" | Ask | “Show me that the MACC discount is actually burning the way finance expects.”<br/>CFO / finance buyer | The CFO wants proof the negotiated discounts are showing up in spend.<br/><br/>If the discount stack is not visible, finance trust drops fast. So does any future MACC negotiation.<br/><br/>Discount verification is a control, not a one-time chart. | kind=ASK_A · addresses=35 verdict=green | None |
| P1.8.B | P1.8 MACC discount verification | #35 "CFO: MACC discount verification" | Implementation | • Use the `YTD` and `MOM` reporting tasks for CFO-facing discount evidence, not the unverified cost-optimization tool surface.<br/>• Verified callable tools include `savings-summary-report`, `commitment-discount-utilization`, `monthly-cost-trend`, `cost-forecasting-model`, and `data-freshness-check` across those reporting tasks.<br/>• The output answers finance questions in order: are the numbers fresh, what did spend do, what does forecast say, and how much commitment coverage is visible in hub data?<br/>• MACC contract attribution beyond the hub and Cost Management data remains with the customer's commercial owner.<br/>• This prevents the executive escalation where finance discovers the discount story does not tie out during review. | The failure pattern is finance asking for discount proof after the quarter narrative has already started.<br/><br/>The reporting tasks put freshness, actuals, forecast, savings summary, and commitment utilization into the recurring CFO view. That does not replace the MACC commercial owner; it gives that owner cleaner evidence before the escalation starts.<br/><br/>Grounding: `ytd-report.yaml`, `mom-report.yaml`, and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=35 verdict=green | None |
| P1.8.C | P1.8 MACC discount verification | #35 "CFO: MACC discount verification" | Details | • Scheduled tasks: `sre-config/scheduled-tasks/ytd-report.yaml` and `sre-config/scheduled-tasks/mom-report.yaml`.<br/>• Owner agents: `chief-financial-officer` for YTD, `finops-practitioner` for MOM.<br/>• Verified tool surface for the discount read:<br/>```<br/>data-freshness-check<br/>monthly-cost-trend<br/>cost-forecasting-model<br/>savings-summary-report<br/>commitment-discount-utilization<br/>```<br/>• Decision pattern:<br/>```<br/>fresh data → finance can use the report<br/>stale data → fix trust before discount review<br/>commitment underused → review coverage and workload persistence<br/>MACC contract question → commercial owner path<br/>```<br/>• On-demand entry points: `srectl scheduledtask invoke ytd-report` and `srectl scheduledtask invoke mom-report`. | This slide avoids the old unsupported tool spine.<br/><br/>The verified reporting tasks provide enough evidence for discount review: freshness, trend, forecast, savings summary, and commitment utilization. Contract burn-down and commercial commitments remain outside the agent.<br/><br/>That split is what keeps the CFO answer defensible. | kind=ASK_C · addresses=35 verdict=green | None |
| P1.9.A | P1.9 Vendor displacement | #47 "Vendor displacement: CloudHealth / Apptio / Vantage / Spot" | Ask | “Why do I still pay CloudHealth, Apptio, Vantage, or Spot if Azure already has the data?”<br/>CFO / procurement lens | Procurement asks why the customer is paying for a third-party tool when Azure has the data.<br/><br/>Every incumbent dashboard waits for a human to log in. The SRE Agent inverts that — autonomous, scheduled, in-tenant, in-Teams. Four words no incumbent can claim today.<br/><br/>Multi-cloud normalization beyond Azure stays with the third party. The comparison stays honest. | kind=ASK_A · addresses=47 verdict=green | None |
| P1.9.B | P1.9 Vendor displacement | #47 "Vendor displacement: CloudHealth / Apptio / Vantage / Spot" | Implementation | • The comparison is not "dashboard versus dashboard"; it is vendor workflow versus in-tenant scheduled control loop.<br/>• The release includes specialist agents, scheduled tasks, a FinOps hub connector, and verified tool surfaces for capacity, cost reporting, AI cost, freshness, commitment, anomaly, and forecast reads.<br/>• The procurement test is workflow-by-workflow: which recurring questions can the customer answer from its own tenant before a ticket or renewal meeting begins?<br/>• Multi-cloud normalization and non-Azure estate management remain outside this Azure-first release.<br/>• Vendor displacement is strongest where the customer values scheduled Azure evidence more than another portal. | The failure pattern is paying for a tool because the team cannot prove which workflows Azure already covers.<br/><br/>The agent changes the diligence motion. Pick a recurring workflow, run the matching task, and compare the output to the renewal requirement. If the in-tenant rhythm covers it, the scope changes. If it does not, the incumbent still has a job.<br/><br/>Grounding: `CATALOG.md` and WorkIQ conversation `3d36e6e4-1a0c-486e-b5cf-692cdfd9bd04`. | kind=ASK_B · addresses=47 verdict=green | None |
| P1.9.C | P1.9 Vendor displacement | #47 "Vendor displacement: CloudHealth / Apptio / Vantage / Spot" | Details | • Operating catalog: `src/templates/sre-agent/CATALOG.md`.<br/>• Compare renewal requirements against verified task outputs, not marketing categories.<br/>• Strong Azure-first lanes:<br/>```<br/>capacity monitoring<br/>hub freshness<br/>monthly and daily cost reporting<br/>AI token cost attribution<br/>commitment utilization and savings summary<br/>forecast and anomaly review<br/>```<br/>• Boundary:<br/>```<br/>multi-cloud normalization → cross-cloud platform owner<br/>vendor-specific workflow parity → customer renewal requirement<br/>production-changing actions → human approval path<br/>```<br/>• On-demand entry point: select one high-value scheduled task and run it against the customer hub. | The mechanism is procurement diligence.<br/><br/>The catalog supplies the recurring tasks. The task output supplies the evidence. The customer renewal requirement supplies the comparison. Nothing here requires claiming feature parity with every vendor module.<br/><br/>That makes the displacement story safer and stronger. | kind=ASK_C · addresses=47 verdict=green | None |
| **P1.10.A** | **Sovereign / Gov cloud** | #60 "Azure Gov / regulated cloud private endpoints" · #72 "Gov / DoD support boundary clarity" | **Ask** | • > "Azure Gov or regulated cloud private endpoints."<br/>• > "Gov or DoD support boundary clarity." | Government and sovereign deployments are high-risk if you over-claim.<br/><br/>Standard-region SKU evidence holds. Azure Gov and DoD validation are not part of this release. Private endpoint readiness depends on the customer's analytics backend.<br/><br/>The boundary is the credibility. | kind=ASK_A · addresses=60,72 verdict=yellow | None |
| **P1.10.B** | **Sovereign / Gov cloud** | #60 "Azure Gov / regulated cloud private endpoints" · #72 "Gov / DoD support boundary clarity" | **Implementation** | • Do not convert standard Azure evidence into sovereign or DoD validation.<br/>• The verified release surface can run SKU and capacity checks where the customer's deployed environment and service endpoints support those tools.<br/>• Private endpoint readiness depends on the customer's analytics backend, identity, network, and service endpoint configuration.<br/>• Gov and DoD support boundaries need the validated government-cloud owner path.<br/>• This is yellow because the agent can provide adjacent evidence, not sovereign attestation. | The failure pattern is overclaiming cloud coverage and creating a support problem later.<br/><br/>The agent can provide capacity and SKU evidence in supported environments. It cannot turn that evidence into government-cloud certification or DoD support approval.<br/><br/>Grounding: memory://work/research/finops-toolkit/practitioner-intent and WorkIQ conversation `3d36e6e4-1a0c-486e-b5cf-692cdfd9bd04`. | kind=ASK_B · addresses=60,72 verdict=yellow | None |
| **P1.10.C** | **Sovereign / Gov cloud** | #60 "Azure Gov / regulated cloud private endpoints" · #72 "Gov / DoD support boundary clarity" | **Details** | • Release-safe statement:<br/>```<br/>standard Azure evidence is not sovereign validation<br/>Gov and DoD readiness require the validated owner path<br/>private endpoint readiness depends on the customer analytics backend and network design<br/>```<br/>• Verified adjacent tool when applicable:<br/>```<br/>sku-availability<br/>```<br/>• Decision pattern:<br/>```<br/>standard Azure SKU question → use the SKU audit where supported<br/>Gov or DoD support question → validated government-cloud owner path<br/>private endpoint question → validate backend networking before commitment<br/>``` | No scheduled task proves sovereign readiness by itself.<br/><br/>That is the answer. Use the SKU audit only for the environment where it is valid, then keep government-cloud and private-endpoint claims on the appropriate owner path.<br/><br/>The boundary prevents a future support case caused by an overstatement in the sales cycle. | kind=ASK_C · addresses=60,72 verdict=yellow | None |
| P1.11.A | EA → MCA migration safety | #52 "EA → MCA migration safety with FTK" | Ask | &gt; "We are moving from EA to MCA."<br/>&gt; "How do I know the Hub still matches the bill?"<br/>&gt; "I cannot lose finance trust on day one." | When customers move from Enterprise Agreement to Microsoft Customer Agreement, finance trust is on the line from day one.<br/><br/>If the hub data does not match the bill after cutover, reconciliation becomes a manual rebuild.<br/><br/>The agent gives you a fast trust verdict before that happens. | kind=ASK_A · addresses=52 verdict=yellow | None |
| P1.11.B | EA → MCA migration safety | #52 "EA → MCA migration safety with FTK" | Implementation | • `HubsHealthCheck` is the verified safety check for finance trust after billing-account change.<br/>• Verified callable tool: `data-freshness-check`.<br/>• `MonitoringScopeValidation` also uses `data-freshness-check`; do not claim unverified export-inventory tooling from this release evidence.<br/>• The cards answer the prevention question: are core hub data functions fresh enough for finance to read before reconciliation begins?<br/>• MCA migration itself remains with the commerce team and the Cost Management export owner. | The support ticket usually appears after finance sees numbers that no longer tie out.<br/><br/>The health check prevents that late discovery by checking freshness before the reconciliation meeting. Scope and commerce cutover remain owner responsibilities, but the agent can flag data trust before the wrong report travels upward.<br/><br/>Grounding: `hubs-health-check.yaml`, `monitoring-scope-validation.yaml`, and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=52 verdict=yellow | None |
| P1.11.C | EA → MCA migration safety | #52 "EA → MCA migration safety with FTK" | Details | • Scheduled tasks: `sre-config/scheduled-tasks/hubs-health-check.yaml` and `sre-config/scheduled-tasks/monitoring-scope-validation.yaml`.<br/>• Owner agent: `ftk-hubs-agent`.<br/>• Verified tool surface:<br/>```<br/>data-freshness-check<br/>```<br/>• Decision pattern:<br/>```<br/>Costs fresh → finance can keep reading current actuals<br/>Costs stale → fix export or ingestion before reconciliation<br/>Transactions empty → diagnose purchase-side export separately<br/>commerce migration issue → commerce and export owner path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke hubs-health-check`. | The mechanics are intentionally narrow: validate freshness first.<br/><br/>A fresh Costs function can be acceptable even with normal Cost Management lag. Empty Transactions is a separate diagnostic, not automatic proof that Costs is stale. Commerce migration and export configuration stay with their owners.<br/><br/>That precision prevents the wrong support case. | kind=ASK_C · addresses=52 verdict=yellow | None |
| 1.99 | End of Part 1 | — | What you've heard / what's next | You heard 11 deal motions: quota is not capacity, CRG cost, RI and SP sequencing, SGS milestone risk, region lock, AOAI quota and GPU forecasting, AOAI honest scope, MACC verification, vendor displacement, sovereign scope, and EA to MCA safety.<br/>Part 2 shifts from deal-entry asks to operating rhythm: token attribution, finance reconciliation, Teams cadence, FinOps Framework mapping, and the run-it-for-them schedule.<br/>Use Part 1 to open the meeting. Use Part 2 to show the system. | That is the deal motion. Eleven question patterns. The agent's answer on each one.<br/><br/>Use these in customer conversations before signature.<br/><br/>Part two is what the customer needs after. | kind=BULLETS | None |

### Table 2 — Part 2: Operate motion (~50 slides)

| # | Cluster | Asks (verbatim) | Title | Content | Speaker notes | Layout | Screenshots / assets |
|---|---|---|---|---|---|---|---|
| 2.0.1 | Intro | — | FinOps toolkit SRE Agent — Part 2 of 2 | • Subtitle: After signature, you run the shop — cost, capacity, cadence, and credibility.<br/>• Audience tag: FinOps Toolkit community in MCAPS, worldwide.<br/>• Co-brand: FinOps Toolkit community + aka.ms/finops/toolkit | After signature, the customer judges the toolkit by what runs every week.<br/><br/>Recurring scheduled tasks. Named tools. Teams delivery. Honest verdicts.<br/><br/>Part two walks the operating motion that earns or loses credibility once the deal is closed. | kind=TITLE | None |
| 2.0.2 | Intro | — | What Part 2 covers | • P2.1 · green · Capacity rhythm · 21,24<br/>• P2.2 · yellow · AOAI token attribution · 25,26,28<br/>• P2.3 · yellow · AI budget brake pedal · 27<br/>• P2.4 · yellow · AI model substitution + prompt economics · 29,30<br/>• P2.5 · green · Daily cost-mover briefing · 36,37<br/>• P2.6 · green · Anomaly detection chain · 41<br/>• P2.7 · yellow · CFO reconciliation trust gap · 32,33,34<br/>• P2.8 · green · Hub freshness verdicts · 38,58,66<br/>• P2.9 · yellow · RI/SP math + portal reconciliation · 62,63<br/>• P2.10 · yellow · Allocation hygiene + post-M&A · 36,38<br/>• P2.11 · green · FinOps Framework operationalization · 43,45<br/>• P2.12 · green · Dashboard adoption → Teams cadence · 70<br/>• P2.13 · green · Native CM gaps (what we exceed) · 48<br/>• P2.14 · green · Run-it-for-them ops model · 55<br/>• P2.15 · red · FTK supportability — HONEST · 49,50,51 | Part two has fifteen operating clusters.<br/><br/>The pattern stays the same: complete answer, partial answer with owner boundary, or honest out-of-scope owner path.<br/><br/>The point is prevention. The scheduled run should find the gap before the customer opens the ticket. | kind=INDEX | None |
| **P2.1.A** | **Capacity rhythm** | #21 "Supply chain forecast depth (30/60/90/180 days?)" · #24 "Proactive capacity monitoring (no real-time API)" | **Ask** | • > "Supply chain forecast depth — 30, 60, 90, 180 days?"<br/>• > "Proactive capacity monitoring."<br/>• > "Do you find the problem before deployment fails?" | Customers ask whether the agent finds capacity problems before deployment fails.<br/><br/>Without a real-time API, the answer is rhythm. Daily, weekly, monthly, quarterly checks that catch drift early.<br/><br/>That cadence is what makes capacity management an operating motion instead of an emergency response. | kind=ASK_A · addresses=21,24 verdict=green | None |
| **P2.1.B** | **Capacity rhythm** | #21 "Supply chain forecast depth (30/60/90/180 days?)" · #24 "Proactive capacity monitoring (no real-time API)" | **Implementation** | • The capacity workstream uses four verified scheduled tasks: `CapacityDailyMonitor`, `CapacityWeeklySupplyReview`, `CapacityMonthlyPlanning`, and `CapacityQuarterlyStrategy`.<br/>• The rhythm separates near-term blockers, broad supply drift, demand planning, and strategic commitment alignment.<br/>• Verified callable tools across the cadence include `vm-quota-usage`, `non-compute-quotas`, `sku-availability`, `capacity-reservation-groups`, `data-freshness-check`, `reservation-recommendation-breakdown`, `commitment-discount-utilization`, `cost-forecasting-model`, and `savings-summary-report`.<br/>• The answer is not a real-time capacity API. The answer is a recurring control loop that catches risk before deployment failure turns into a support ticket.<br/>• Teams delivery depends on connector setup and task-agent tool availability after deployment. | The failure pattern is late discovery. The inversion is cadence.<br/><br/>Daily checks catch immediate quota, SKU, CRG, non-compute, and freshness signals. Weekly review broadens the supply picture. Monthly planning brings forecast and commitment context. Quarterly strategy separates supply risk from commercial decision making.<br/><br/>Grounding: `00-tool-task-agent-truth.md` and `memory://thinking/sequential/2026/05/03/session-1777846402443-12272`. | kind=ASK_B · addresses=21,24 verdict=green | None |
| **P2.1.C** | **Capacity rhythm** | #21 "Supply chain forecast depth (30/60/90/180 days?)" · #24 "Proactive capacity monitoring (no real-time API)" | **Details** | • Scheduled task paths:<br/>```<br/>sre-config/scheduled-tasks/capacity-daily-monitor.yaml<br/>sre-config/scheduled-tasks/capacity-weekly-supply-review.yaml<br/>sre-config/scheduled-tasks/capacity-monthly-planning.yaml<br/>sre-config/scheduled-tasks/capacity-quarterly-strategy.yaml<br/>```<br/>• Owner agent: `azure-capacity-manager`.<br/>• Decision pattern:<br/>```<br/>daily signal → immediate quota, SKU, CRG, non-compute, or freshness risk<br/>weekly signal → broader supply drift or recommendation review<br/>monthly signal → forecast, commitment, and capacity-planning review<br/>quarterly signal → strategic supply and rate-optimization alignment<br/>```<br/>• On-demand entry points: invoke the matching scheduled task for the customer hub. | Four task files create one preventive loop.<br/><br/>The agent does not need a fictional capacity oracle. It needs enough real signals, on a recurring rhythm, to show whether the customer is facing quota pressure, SKU restriction, CRG mismatch, non-compute risk, freshness risk, or commitment misalignment.<br/><br/>That is how the ticket is prevented. | kind=ASK_C · addresses=21,24 verdict=green | None |
| P2.2.A | P2.2 AOAI token attribution | #25 "AOAI token attribution per user / app / team" · #26 "Token → dollar translation + reconciliation" · #28 "Near-real-time AI cost observability" | Ask | “Show me who is burning AOAI tokens, what that means in dollars, and whether I can see it before month-end.”<br/>Finance and AI workload owner | Finance cannot manage AI spend if token cost has no application or team owner.<br/><br/>Customers want attribution, dollar translation, and earlier visibility than a month-end surprise.<br/><br/>The agent supplies the cost and application evidence it owns, then names the boundary for user-level attribution and live platform telemetry. | kind=ASK_A · addresses=25,26,28 verdict=yellow | None |
| P2.2.B | P2.2 AOAI token attribution | #25 "AOAI token attribution per user / app / team" · #26 "Token → dollar translation + reconciliation" · #28 "Near-real-time AI cost observability" | Implementation | • `AIWorkloadCostAnalysis` is the verified AI cost task for this cluster.<br/>• Verified callable tools: `ai-token-usage-breakdown`, `ai-model-cost-comparison`, `ai-daily-trend`, and `ai-cost-by-application`.<br/>• The task separates token volume, model mix, daily cost trend, and application attribution before finance opens a vague AI-spend ticket.<br/>• User-level attribution and true near-real-time observability depend on source data that this release does not prove.<br/>• This is yellow because the agent gives the cost story, not every identity or live telemetry answer. | The failure pattern is an AI bill with no owner.<br/><br/>The AI workload task brings four signals into one review: token volume, model mix, daily trend, and application attribution. That is enough to prevent the first support path from being "AI is expensive and nobody knows why."<br/><br/>Grounding: `ai-workload-cost-analysis.yaml` and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=25,26,28 verdict=yellow | None |
| P2.2.C | P2.2 AOAI token attribution | #25 "AOAI token attribution per user / app / team" · #26 "Token → dollar translation + reconciliation" · #28 "Near-real-time AI cost observability" | Details | • Scheduled task: `sre-config/scheduled-tasks/ai-workload-cost-analysis.yaml`.<br/>• Owner agent: `chief-financial-officer`.<br/>• Verified tool surface:<br/>```<br/>ai-token-usage-breakdown<br/>ai-model-cost-comparison<br/>ai-daily-trend<br/>ai-cost-by-application<br/>```<br/>• Decision pattern:<br/>```<br/>token cost rising → inspect model mix and application owner<br/>application attribution missing → fix ownership before chargeback<br/>daily trend spike → inspect AI workload behavior before month-end close<br/>user-level identity request → source-data and platform owner path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke ai-workload-cost-analysis`. | The mechanics stay inside the verified surface.<br/><br/>Token breakdown explains volume. Model comparison explains unit economics. Daily trend explains whether the problem is moving now. Cost by application tells finance where ownership starts.<br/><br/>Anything below that grain needs the customer's telemetry and platform owner. | kind=ASK_C · addresses=25,26,28 verdict=yellow | None |
| P2.3.A | AI budget brake pedal | #27 "AI budget hard guardrails (brake pedal, not alerts)" | Ask | &gt; "Can I put a hard brake on AI spend?"<br/>&gt; "I do not want a dev project to run away for three days."<br/>&gt; "Alerts are not the same as a stop button." | Customers ask for a hard stop on AI spend.<br/><br/>Alerts are not stops. The agent improves alerting and routing, but the kill switch is not built into Cost Management or this agent.<br/><br/>That answer protects trust later. Better to scope it now than be wrong on day thirty. | kind=ASK_A · addresses=27 verdict=yellow | None |
| P2.3.B | AI budget brake pedal | #27 "AI budget hard guardrails (brake pedal, not alerts)" | Implementation | • The preventive answer is honest: this release does not prove a hard kill switch for Azure OpenAI spend.<br/>• Verified evidence surfaces for the early-warning side are `AIWorkloadCostAnalysis`, `MOM`, and `YTD`.<br/>• Verified callable tools include `ai-daily-trend`, `ai-cost-by-application`, `cost-anomaly-detection`, `cost-forecasting-model`, `monthly-cost-trend`, and `data-freshness-check`.<br/>• `BudgetCoverageAudit` exists as a scheduled governance task, but the truth table does not verify callable budget-control tools for this deck claim.<br/>• The agent can prevent the wrong ticket by separating alerting, forecasting, and application ownership from a platform-enforced stop. | The failure pattern is promising a brake pedal when the release can prove warning and attribution.<br/><br/>The agent can show AI trend, application owner, anomaly, forecast, and data freshness. That prevents a vague escalation and makes the platform-owner ask precise.<br/><br/>Grounding: `budget-coverage-audit.yaml`, `ai-workload-cost-analysis.yaml`, `mom-report.yaml`, `ytd-report.yaml`, and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=27 verdict=yellow | None |
| P2.3.C | AI budget brake pedal | #27 "AI budget hard guardrails (brake pedal, not alerts)" | Details | • Scheduled task: `sre-config/scheduled-tasks/budget-coverage-audit.yaml`.<br/>• Owner agent: `finops-practitioner`.<br/>• Verified supporting task surfaces:<br/>```<br/>AIWorkloadCostAnalysis → ai-daily-trend, ai-cost-by-application<br/>MOM → cost-anomaly-detection, cost-forecasting-model, monthly-cost-trend<br/>YTD → cost-forecasting-model, cost-by-financial-hierarchy, data-freshness-check<br/>```<br/>• Decision pattern:<br/>```<br/>AI cost rising → identify application and trend<br/>forecast risk → escalate budget owner before month-end<br/>hard stop request → platform and policy owner path<br/>missing verified budget tool → do not claim automated brake<br/>```<br/>• On-demand entry points: invoke the AI cost, MOM, YTD, or budget audit task that matches the evidence needed. | The mechanics protect credibility.<br/><br/>Cost trend, forecast, anomaly, application attribution, and freshness are verified. A hard stop is not. Budget-governance evidence can still help the owner, but the deck must not imply an automated spend stop that the release cannot prove.<br/><br/>That is why the answer stays yellow. | kind=ASK_C · addresses=27 verdict=yellow | None |
| P2.4.A | AI model substitution + prompt economics | #29 "Model substitution for cost (route to cheaper)" · #30 "Prompt optimization as cost lever (KV caching)" | Ask | &gt; "Which model should my customer keep?"<br/>&gt; "Which model should they stop paying for?"<br/>&gt; "Can prompt design move the bill?" | The customer wants to know which model to keep, which to drop, and whether prompt design changes the bill.<br/><br/>The agent quantifies the lever. The application owner validates the change.<br/><br/>That split is what keeps optimization safe. | kind=ASK_A · addresses=29,30 verdict=yellow | None |
| P2.4.B | AI model substitution + prompt economics | #29 "Model substitution for cost (route to cheaper)" · #30 "Prompt optimization as cost lever (KV caching)" | Implementation | • `AIWorkloadCostAnalysis` provides the verified evidence for substitution and prompt-economics review.<br/>• Verified callable tools: `ai-model-cost-comparison`, `ai-token-usage-breakdown`, `ai-cost-by-application`, and `ai-daily-trend`.<br/>• The task identifies model cost, token volume, application owner, and trend before anyone proposes a model change.<br/>• The agent surfaces candidates; the application owner validates quality, latency, safety, and deployment behavior.<br/>• This is yellow because the release does not rewrite prompts, switch deployments, or guarantee model behavior. | The failure pattern is treating cost ranking as permission to change the workload.<br/><br/>The agent can show which models cost more, which applications own the spend, and whether token behavior is changing. That evidence prevents a vague optimization ticket.<br/><br/>The application owner still owns the model swap or prompt change. | kind=ASK_B · addresses=29,30 verdict=yellow | None |
| P2.4.C | AI model substitution + prompt economics | #29 "Model substitution for cost (route to cheaper)" · #30 "Prompt optimization as cost lever (KV caching)" | Details | • Scheduled task: `sre-config/scheduled-tasks/ai-workload-cost-analysis.yaml`.<br/>• Owner agent: `chief-financial-officer`.<br/>• Verified tool surface:<br/>```<br/>ai-model-cost-comparison<br/>ai-token-usage-breakdown<br/>ai-cost-by-application<br/>ai-daily-trend<br/>```<br/>• Decision pattern:<br/>```<br/>high model spend → inspect application owner and model mix<br/>token volume shift → inspect workload behavior before recommending change<br/>candidate cheaper lane → application owner validation<br/>prompt or model rewrite → outside this release<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke ai-workload-cost-analysis`. | The L400 point is evidence order.<br/><br/>Start with model cost. Add token volume. Add application owner. Add trend. Only then discuss whether a cheaper lane or prompt change deserves a workload-owner review.<br/><br/>The agent identifies the candidate; it does not perform the change. | kind=ASK_C · addresses=29,30 verdict=yellow | None |
| P2.5.A | Daily cost-mover briefing | #36 "Post-M&A tagging chaos + allocation" · #37 "Chargeback design: amortized vs billed" | Ask | &gt; "The org chart changed after M&amp;A."<br/>&gt; "Give me the daily cost movers and something I can use for showback."<br/>&gt; "Do not make me wait for month-end." | After an acquisition, the org chart changed and finance cannot wait until month-end to see what moved.<br/><br/>The daily card gives them a working showback signal before the data is perfect.<br/><br/>Working signal beats perfect data when the close is already running. | kind=ASK_A · addresses=36,37 verdict=green | None |
| P2.5.B | Daily cost-mover briefing | #36 "Post-M&A tagging chaos + allocation" · #37 "Chargeback design: amortized vs billed" | Implementation | • `MOM` is the verified recurring cost-mover task for this cluster.<br/>• Verified callable tools include `data-freshness-check`, `monthly-cost-trend`, `top-services-by-cost`, `top-resource-groups-by-cost`, `cost-anomaly-detection`, `cost-by-region-trend`, `cost-forecasting-model`, `commitment-discount-utilization`, and `savings-summary-report`.<br/>• The task answers the prevention questions in order: is the data fresh, what moved, which services and resource groups drove it, is it anomalous, and where is the forecast heading?<br/>• Allocation and chargeback design still require customer policy choices for billed, effective, and amortized views.<br/>• Teams delivery follows the task delivery guard when connector and tool availability are present. | The failure pattern is waiting until close to discover a mover.<br/><br/>The MOM task checks freshness first, then trend, services, resource groups, anomaly signal, region, forecast, savings, and commitment context. That turns an allocation surprise into a daily review item.<br/><br/>Grounding: `mom-report.yaml` and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=36,37 verdict=green | None |
| P2.5.C | Daily cost-mover briefing | #36 "Post-M&A tagging chaos + allocation" · #37 "Chargeback design: amortized vs billed" | Details | • Scheduled task: `sre-config/scheduled-tasks/mom-report.yaml`.<br/>• Owner agent: `finops-practitioner`.<br/>• Verified tool surface:<br/>```<br/>data-freshness-check<br/>monthly-cost-trend<br/>top-services-by-cost<br/>top-resource-groups-by-cost<br/>cost-anomaly-detection<br/>cost-forecasting-model<br/>cost-by-region-trend<br/>savings-summary-report<br/>commitment-discount-utilization<br/>```<br/>• Decision pattern:<br/>```<br/>fresh data → read trend and movers<br/>service or resource-group spike → inspect owner and workload context<br/>anomaly signal → investigate before finance review<br/>chargeback method question → customer finance policy path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke mom-report`. | The mechanics make the review repeatable.<br/><br/>Freshness gates trust. Trends show movement. Service and resource-group cuts locate the mover. Anomaly and forecast show whether action is urgent. Savings and commitment context keep rate optimization from being separated from allocation.<br/><br/>Chargeback policy remains a finance decision. | kind=ASK_C · addresses=36,37 verdict=green | None |
| P2.6.A | Anomaly detection chain | #41 "Cost reporting pipeline readiness" | Ask | &gt; "Is the reporting pipe ready enough for anomaly automation?"<br/>&gt; "What is missing right now?"<br/>&gt; "What do I deploy next?" | Customers want to know if the reporting pipe is ready for anomaly automation.<br/><br/>The honest sequence is audit first, fix routing second, deploy missing alerts third, then automate. Skipping the audit creates noise that erodes trust.<br/><br/>Order of operations matters here. | kind=ASK_A · addresses=41 verdict=green | None |
| P2.6.B | Anomaly detection chain | #41 "Cost reporting pipeline readiness" | Implementation | • The verified anomaly evidence in this release comes from `MOM`, not from claiming unverified alert-deployment tools.<br/>• Verified callable tools include `data-freshness-check`, `cost-anomaly-detection`, `monthly-cost-trend`, `top-services-by-cost`, `top-resource-groups-by-cost`, and `cost-forecasting-model`.<br/>• `AlertCoverageAudit` exists as a scheduled audit, but the truth table does not verify its callable alert-inventory tool surface for this deck claim.<br/>• The prevention sequence is freshness first, anomaly signal second, driver context third, alert-coverage owner path fourth.<br/>• This keeps anomaly automation from starting on stale or incomplete reporting data. | The failure pattern is automating on bad data.<br/><br/>MOM proves the reporting side first: freshness, trend, anomaly signal, services, resource groups, and forecast. Alert coverage can still be reviewed by its owner, but the deck does not need an unsupported deploy claim to answer readiness.<br/><br/>Grounding: `mom-report.yaml`, `alert-coverage-audit.yaml`, and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=41 verdict=green | None |
| P2.6.C | Anomaly detection chain | #41 "Cost reporting pipeline readiness" | Details | • Scheduled tasks:<br/>```<br/>sre-config/scheduled-tasks/mom-report.yaml<br/>sre-config/scheduled-tasks/alert-coverage-audit.yaml<br/>```<br/>• Verified MOM tool surface:<br/>```<br/>data-freshness-check<br/>cost-anomaly-detection<br/>monthly-cost-trend<br/>top-services-by-cost<br/>top-resource-groups-by-cost<br/>cost-forecasting-model<br/>```<br/>• Decision pattern:<br/>```<br/>fresh hub data → anomaly signal can be trusted<br/>stale hub data → fix ingestion before automation<br/>anomaly without driver context → inspect service and resource-group movers<br/>alert coverage gap → monitoring owner path<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke mom-report` for data and anomaly readiness. | The chain starts with trust.<br/><br/>If the hub is stale, no alert coverage story is credible. If the anomaly signal exists but driver context is missing, finance still cannot act. MOM gives the data readiness side; the alert audit owner handles alert inventory and routing.<br/><br/>That split prevents noisy automation. | kind=ASK_C · addresses=41 verdict=green | None |
| P2.7.A | P2.7 CFO reconciliation trust gap | #32 "CFO reconciliation trust gap (portal / invoice / FOCUS)" · #33 "CFO: AI spend forecast + brake pedal, not last month" · #34 "Multi-tenant / multi-cloud single pane" | Ask | “Can finance trust the numbers, can I see where AI spend is heading, and can I get one pane across the estate?”<br/>CFO / controller / FinOps lead | Finance cannot close the books while teams argue about which Azure number is real.<br/><br/>Trusted actuals, forecast, and one estate view are the basis for every quarter narrative.<br/><br/>The agent gives the Azure hub evidence it owns and names the cross-cloud boundary. | kind=ASK_A · addresses=32,33,34 verdict=yellow | None |
| P2.7.B | P2.7 CFO reconciliation trust gap | #32 "CFO reconciliation trust gap (portal / invoice / FOCUS)" · #33 "CFO: AI spend forecast + brake pedal, not last month" · #34 "Multi-tenant / multi-cloud single pane" | Implementation | • `YTD` and `MOM` are the verified finance reporting tasks for this cluster.<br/>• `YTD` is owned by `chief-financial-officer`; `MOM` is owned by `finops-practitioner`.<br/>• Verified callable tools include `data-freshness-check`, `monthly-cost-trend`, `cost-forecasting-model`, `monthly-cost-change-percentage`, `cost-by-financial-hierarchy`, `quarterly-cost-by-resource-group`, `savings-summary-report`, and `commitment-discount-utilization`.<br/>• The tasks answer close questions: is the data fresh, what changed, where is forecast heading, where are allocation gaps, and how are commitments performing?<br/>• Multi-cloud normalization beyond Azure remains a customer platform and commerce owner path. | The failure pattern is a finance meeting that starts with dueling numbers.<br/><br/>YTD and MOM make the first question data trust. Then they move to trend, forecast, hierarchy, savings, commitment, and volatility. That prevents the finance review from becoming a support ticket about which number is real.<br/><br/>Grounding: `ytd-report.yaml`, `mom-report.yaml`, and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=32,33,34 verdict=yellow | None |
| P2.7.C | P2.7 CFO reconciliation trust gap | #32 "CFO reconciliation trust gap (portal / invoice / FOCUS)" · #33 "CFO: AI spend forecast + brake pedal, not last month" · #34 "Multi-tenant / multi-cloud single pane" | Details | • Scheduled tasks:<br/>```<br/>sre-config/scheduled-tasks/ytd-report.yaml<br/>sre-config/scheduled-tasks/mom-report.yaml<br/>```<br/>• Owner agents:<br/>```<br/>YTD → chief-financial-officer<br/>MOM → finops-practitioner<br/>```<br/>• Decision pattern:<br/>```<br/>freshness verdict → trust or block the finance read<br/>actuals and trend → explain movement<br/>forecast → show forward risk<br/>savings and commitment → explain rate optimization<br/>financial hierarchy → expose allocation gaps<br/>cross-cloud pane → platform and commerce owner path<br/>```<br/>• On-demand entry points: `srectl scheduledtask invoke ytd-report` and `srectl scheduledtask invoke mom-report`. | The mechanics are finance-safe because the first gate is freshness.<br/><br/>If freshness is green, use actuals, trend, forecast, savings, commitment, and financial hierarchy. If freshness is not green, stop the finance read and fix the data path.<br/><br/>Cross-cloud remains a boundary, not an implied feature. | kind=ASK_C · addresses=32,33,34 verdict=yellow | None |
| P2.8.A | Hub freshness verdicts | #38 "Tagging inheritance workarounds (0% coverage)" · #58 "Pipeline failures / stale data / msexports → ingestion" · #66 "Finance trust / data credibility (60-day discrepancy)" | Ask | &gt; "Are the numbers fresh?"<br/>&gt; "Did the pipeline miss anything?"<br/>&gt; "Can finance trust this before the QBR starts?" | Before a QBR, finance needs to know whether the hub data is current and complete.<br/><br/>A clean verdict before the meeting beats a manual reconciliation during it.<br/><br/>The agent produces that verdict daily. | kind=ASK_A · addresses=38,58,66 verdict=green | None |
| P2.8.B | Hub freshness verdicts | #38 "Tagging inheritance workarounds (0% coverage)" · #58 "Pipeline failures / stale data / msexports → ingestion" · #66 "Finance trust / data credibility (60-day discrepancy)" | Implementation | • `HubsHealthCheck` and `MonitoringScopeValidation` are the verified hub trust tasks for this cluster.<br/>• Verified callable tool: `data-freshness-check`.<br/>• `HubsHealthCheck` treats `Costs()` freshness as the primary trust signal and reports `Transactions()` zero-row diagnostics without falsely marking core cost freshness stale.<br/>• `MonitoringScopeValidation` adds the monitoring-scope workflow, but the verified deck claim remains the freshness tool unless the deployed agent inventory proves more.<br/>• The prevention move is to gate finance and QBR reads on freshness before a stale-data discrepancy becomes an escalation. | The failure pattern is a finance discrepancy that turns into a hub ticket during the meeting.<br/><br/>The health check gives a trust gate before the meeting. It distinguishes stale `Costs()` from an empty optional function and calls out `Transactions()` as a separate diagnostic lane.<br/><br/>Grounding: `hubs-health-check.yaml`, `monitoring-scope-validation.yaml`, and `00-tool-task-agent-truth.md`. | kind=ASK_B · addresses=38,58,66 verdict=green | None |
| P2.8.C | Hub freshness verdicts | #38 "Tagging inheritance workarounds (0% coverage)" · #58 "Pipeline failures / stale data / msexports → ingestion" · #66 "Finance trust / data credibility (60-day discrepancy)" | Details | • Scheduled tasks:<br/>```<br/>sre-config/scheduled-tasks/hubs-health-check.yaml<br/>sre-config/scheduled-tasks/monitoring-scope-validation.yaml<br/>```<br/>• Owner agent: `ftk-hubs-agent`.<br/>• Verified tool surface:<br/>```<br/>data-freshness-check<br/>```<br/>• Decision pattern:<br/>```<br/>Costs() fresh → continue finance read<br/>Costs() stale or empty → investigate exports and ingestion<br/>Transactions() zero rows → transaction analysis unavailable until export and function path are verified<br/>Prices() or Recommendations() empty → follow-up item, not automatic core cost staleness<br/>```<br/>• On-demand entry point: `srectl scheduledtask invoke hubs-health-check`. | The details matter because false staleness claims create noise.<br/><br/>`Costs()` is the primary freshness signal. `Transactions()` is separate. Empty `Prices()` or `Recommendations()` can be a follow-up without blocking the core cost read.<br/><br/>That precision prevents a bad diagnostic from becoming the ticket. | kind=ASK_C · addresses=38,58,66 verdict=green | None |
| P2.9.A | RI/SP math + portal reconciliation | #62 "RI / Savings Plans math in Hub (effective rate over time)" · #63 "Portal reconciliation (Kusto vs portal numbers)" | Ask | &gt; "Show me the RI and savings-plan math in the Hub."<br/>&gt; "Then show me why it does or does not match the portal."<br/>&gt; "Do not hand me two different stories." | Finance wants the savings math from the hub and proof it ties out to the portal.<br/><br/>Two stories from one source breaks trust fast. The agent gives both — opportunity math and reconciliation — with a clear boundary when reconciliation cannot close.<br/><br/>That separation is the trust contract. | kind=ASK_A · addresses=62,63 verdict=yellow | None |
| P2.9.B | RI/SP math + portal reconciliation | #62 "RI / Savings Plans math in Hub (effective rate over time)" · #63 "Portal reconciliation (Kusto vs portal numbers)" | Implementation | • The verified rate-optimization evidence comes from `CapacityMonthlyPlanning`, `CapacityQuarterlyStrategy`, `YTD`, and `MOM`.<br/>• Verified callable tools include `commitment-discount-utilization`, `savings-summary-report`, `reservation-recommendation-breakdown`, `cost-forecasting-model`, `monthly-cost-trend`, and `data-freshness-check`.<br/>• A separate recommendation-review task exists, but its prompt-mentioned recommendation tool is not verified as callable for this deck claim.<br/>• Portal reconciliation depends on export completeness, especially `Transactions()` availability for purchase-side proof.<br/>• The agent ranks evidence and flags gaps; finance and workload owners still approve purchases. | The failure pattern is blending opportunity math and reconciliation proof.<br/><br/>The agent can show utilization, savings, reservation recommendation breakdown, forecast, and trend. It can also tell when `Transactions()` is unavailable. That makes reconciliation a data-quality question instead of an argument about the portal.<br/><br/>Grounding: capacity tasks, YTD, MOM, and the truth table. | kind=ASK_B · addresses=62,63 verdict=yellow | None |
| P2.9.C | RI/SP math + portal reconciliation | #62 "RI / Savings Plans math in Hub (effective rate over time)" · #63 "Portal reconciliation (Kusto vs portal numbers)" | Details | • Scheduled tasks for verified evidence:<br/>```<br/>sre-config/scheduled-tasks/capacity-monthly-planning.yaml<br/>sre-config/scheduled-tasks/capacity-quarterly-strategy.yaml<br/>sre-config/scheduled-tasks/ytd-report.yaml<br/>sre-config/scheduled-tasks/mom-report.yaml<br/>```<br/>• Decision pattern:<br/>```<br/>utilization and savings → rate optimization posture<br/>reservation recommendation breakdown → candidate shape<br/>forecast and trend → timing risk<br/>Transactions() zero rows → reconciliation unavailable until export path is fixed<br/>purchase approval → finance and workload owner path<br/>```<br/>• On-demand entry points: invoke the capacity planning or finance reporting task that matches the question. | Keep the math lanes separate.<br/><br/>Utilization and savings explain current benefit. Recommendation breakdown explains candidates. Forecast and trend explain timing. `Transactions()` explains whether purchase-side reconciliation can be trusted.<br/><br/>Approval remains outside the agent. | kind=ASK_C · addresses=62,63 verdict=yellow | None |
| P2.10.A | Allocation hygiene + post-M&A | #36 "Post-M&A tagging chaos + allocation" · #38 "Tagging inheritance workarounds (0% coverage)" | Ask | &gt; "The tags are broken after M&amp;A."<br/>&gt; "Can you tell me what we cannot allocate?"<br/>&gt; "Do not promise auto-remediation if it is not there." | Tags break after acquisitions.<br/><br/>Customers want to know what they cannot allocate today. They do not want false promises about auto-remediation.<br/><br/>The agent measures the gap and routes the cleanup. The customer's governance team owns the repair. | kind=ASK_A · addresses=36,38 verdict=yellow | None |
| P2.10.B | Allocation hygiene + post-M&A | #36 "Post-M&A tagging chaos + allocation" · #38 "Tagging inheritance workarounds (0% coverage)" | Implementation | • The verified allocation evidence comes from `YTD` and `MOM` finance reporting, with `BudgetCoverageAudit` as an existing governance task whose callable tool surface is not verified by the truth table.<br/>• Verified callable tools include `cost-by-financial-hierarchy`, `top-resource-groups-by-cost`, `monthly-cost-trend`, and `data-freshness-check`.<br/>• The tasks quantify spend through financial hierarchy and resource-group movers before governance cleanup starts.<br/>• The agent does not auto-remediate tags, infer acquisition ownership as fact, or change policy.<br/>• This is yellow because allocation quality depends on customer tag policy and source data. | The failure pattern is promising tag repair when the release can prove spend-weighted allocation evidence.<br/><br/>YTD and MOM show financial hierarchy, resource-group movers, trend, and freshness. That is enough to prioritize cleanup before chargeback.<br/><br/>The customer governance team owns the repair. | kind=ASK_B · addresses=36,38 verdict=yellow | None |
| P2.10.C | Allocation hygiene + post-M&A | #36 "Post-M&A tagging chaos + allocation" · #38 "Tagging inheritance workarounds (0% coverage)" | Details | • Scheduled tasks:<br/>```<br/>sre-config/scheduled-tasks/ytd-report.yaml<br/>sre-config/scheduled-tasks/mom-report.yaml<br/>sre-config/scheduled-tasks/budget-coverage-audit.yaml<br/>```<br/>• Verified finance tool surface:<br/>```<br/>cost-by-financial-hierarchy<br/>top-resource-groups-by-cost<br/>monthly-cost-trend<br/>data-freshness-check<br/>```<br/>• Decision pattern:<br/>```<br/>fresh data → allocation read can proceed<br/>financial hierarchy gap → governance cleanup path<br/>high-cost resource-group mover → prioritize owner review<br/>tag policy or auto-remediation → customer governance owner path<br/>```<br/>• On-demand entry points: invoke YTD or MOM for spend-weighted allocation evidence. | The mechanics stay spend-weighted.<br/><br/>Freshness tells whether the read can be trusted. Financial hierarchy shows allocation. Resource-group movers show where cleanup matters most. Budget coverage can support governance, but tag repair remains outside the verified agent action.<br/><br/>That keeps chargeback conversations honest. | kind=ASK_C · addresses=36,38 verdict=yellow | None |
| P2.11.A | FinOps Framework operationalization | #43 "FinOps maturity assessment (Crawl / Walk / Run)" · #45 "FinOps Framework operationalization" | Ask | &gt; "Do not give me a maturity poster."<br/>&gt; "Show me the operating model."<br/>&gt; "What runs daily, weekly, monthly, and quarterly?" | Customers do not want another maturity poster.<br/><br/>They want the operating model — what runs daily, weekly, monthly, quarterly — and who owns it.<br/><br/>The catalog is that operating map. Maturity is the scorecard, not the work. | kind=ASK_A · addresses=43,45 verdict=green | None |
| P2.11.B | FinOps Framework operationalization | #43 "FinOps maturity assessment (Crawl / Walk / Run)" · #45 "FinOps Framework operationalization" | Implementation | • The operating model is the recurring task catalog, not a maturity poster alone.<br/>• `src/templates/sre-agent/CATALOG.md` maps recurring work across budgeting, forecasting, allocation, anomaly management, unit economics, rate optimization, AI cost, and capacity operations.<br/>• The deployed SRE Agent package includes five subagents, three skills, thirty-three tools, eighteen scheduled tasks, and the FinOps Hub Kusto connector.<br/>• Customer maturity claims must come from customer task evidence, not a static chart.<br/>• The first rollout should start with a small task set and expand after the cadence earns trust. | The failure pattern is maturity theatre.<br/><br/>The catalog turns FinOps Framework language into an operating rhythm. The README gives the concrete package shape: subagents, skills, tools, scheduled tasks, and connector. Customer posture still comes from customer evidence.<br/><br/>Grounding: `CATALOG.md` and `src/templates/sre-agent/README.md`. | kind=ASK_B · addresses=43,45 verdict=green | None |
| P2.11.C | FinOps Framework operationalization | #43 "FinOps maturity assessment (Crawl / Walk / Run)" · #45 "FinOps Framework operationalization" | Details | • Catalog: `src/templates/sre-agent/CATALOG.md`.<br/>• Deployment package reference: `src/templates/sre-agent/README.md`.<br/>• Operating bands:<br/>```<br/>daily and weekly reporting<br/>monthly and quarterly finance review<br/>capacity monitoring and planning<br/>AI workload cost analysis<br/>hub health and governance audits<br/>```<br/>• Decision pattern:<br/>```<br/>measured gap → select task<br/>task output → assign owner action<br/>stable cadence → expand task set<br/>no customer evidence → no maturity claim<br/>```<br/>• First-run task set: pick three customer-relevant tasks and wire them into the operating calendar. | The catalog is the map, not the destination.<br/><br/>Pick tasks that match measured gaps. Assign owners from the task output. Expand only after the cadence works.<br/><br/>That is how the maturity conversation becomes operations. | kind=ASK_C · addresses=43,45 verdict=green | None |
| P2.12.A | Dashboard adoption → Teams cadence | #70 "Dashboard adoption failure (80% never opened by finance)" | Ask | &gt; "Finance never opened the dashboard."<br/>&gt; "How do I get this into the weekly rhythm?"<br/>&gt; "Send me what matters where the team already works." | Finance never opened the dashboard.<br/><br/>The fix is delivering what matters where the team already works — Teams. The agent posts the operating brief there once setup is complete.<br/><br/>Adoption is a workflow problem, not a visualization problem. | kind=ASK_A · addresses=70 verdict=green | None |
| P2.12.B | Dashboard adoption → Teams cadence | #70 "Dashboard adoption failure (80% never opened by finance)" | Implementation | • The adoption pattern is not another dashboard tab. It is scheduled task output in the team's collaboration rhythm.<br/>• The SRE Agent README states Outlook and Teams notification connectors are optional and require portal-based OAuth setup after deployment.<br/>• Scheduled task prompts include a Teams delivery guard: use Teams only when the connector, channel, and delivery tool are available; otherwise return the completed report in task output.<br/>• The operating artifact is the card or report; the dashboard becomes drill-down context.<br/>• This is green as an operating pattern with setup, not as an automatic default provisioned by `azd up`. | The failure pattern is building a dashboard nobody opens.<br/><br/>The README is explicit: Teams notification is optional and configured after deployment. The task prompts are also explicit: deliver to Teams only when the connector and tool are available.<br/><br/>That makes the adoption story honest. | kind=ASK_B · addresses=70 verdict=green | None |
| P2.12.C | Dashboard adoption → Teams cadence | #70 "Dashboard adoption failure (80% never opened by finance)" | Details | • Deployment reference: `src/templates/sre-agent/README.md`.<br/>• Post-deploy setup requirement:<br/>```<br/>Add Teams connector in the Azure SRE Agent portal<br/>Sign in through OAuth<br/>Paste the channel URL<br/>```<br/>• Task prompt guard:<br/>```<br/>if Teams connector and delivery tool are available → send final report to Teams<br/>if unavailable or unauthorized → return completed report in run output<br/>do not probe alternate webhook or Graph paths<br/>```<br/>• Decision pattern: treat task output as the operating artifact and dashboards as follow-up context.<br/>• First validation: invoke one low-risk task and confirm the report lands through the configured delivery path. | The mechanics are setup-bound.<br/><br/>The agent package does not provision Teams notification by default. Configure it, validate one task, then move finance review to the operating report.<br/><br/>That is the clean adoption lane. | kind=ASK_C · addresses=70 verdict=green | None |
| P2.13.A | Native CM gaps (what we exceed) | #48 "Azure CM native gaps (hourly visibility, anomaly)" | Ask | &gt; "Azure Cost Management already gives me budgets and charts."<br/>&gt; "What exactly do you add on top?"<br/>&gt; "Show me the gap, not the slogan." | Customers ask why they need this if Azure Cost Management is right there.<br/><br/>Use native where it is enough. Use the agent where native needs an operating loop: scheduled evidence, freshness gates, cross-signal triage, and owner actions.<br/><br/>That positioning earns trust faster than dismissing native tools. | kind=ASK_A · addresses=48 verdict=green | None |
| P2.13.B | Native CM gaps (what we exceed) | #48 "Azure CM native gaps (hourly visibility, anomaly)" | Implementation | • The added value is the scheduled control loop, not a claim that native Cost Management is replaced.<br/>• Verified loop pieces include `HubsHealthCheck` for data trust, `MOM` for anomaly and mover context, `YTD` for finance close context, capacity tasks for quota and supply risk, and `AIWorkloadCostAnalysis` for AI unit-economics evidence.<br/>• `AlertCoverageAudit` and `AdvisorSuppressionReview` exist as governance tasks, but the truth table does not verify their callable tool surface for this deck claim.<br/>• The agent exceeds a dashboard by assembling recurring evidence and owner actions before the ticket exists.<br/>• Native tools remain the baseline; the agent is the preventive operating layer. | The failure pattern is selling against native tools instead of clarifying the layer.<br/><br/>Native Cost Management remains the baseline. The agent adds a recurring in-tenant loop: freshness, anomaly context, finance view, capacity risk, and AI cost evidence.<br/><br/>Grounding: README package shape, scheduled tasks, and truth table. | kind=ASK_B · addresses=48 verdict=green | None |
| P2.13.C | Native CM gaps (what we exceed) | #48 "Azure CM native gaps (hourly visibility, anomaly)" | Details | • Verified preventive loop examples:<br/>```<br/>hubs-health-check → data trust gate<br/>mom-report → anomaly, mover, forecast, savings, commitment context<br/>ytd-report → fiscal close and forecast context<br/>capacity-daily-monitor → quota, SKU, CRG, non-compute, freshness risk<br/>ai-workload-cost-analysis → AI cost, model, token, application evidence<br/>```<br/>• Governance tasks present in source:<br/>```<br/>alert-coverage-audit.yaml<br/>advisor-suppression-review.yaml<br/>```<br/>• Boundary: do not claim write automation or callable governance tools unless the deployed agent inventory verifies them.<br/>• Decision pattern: use native views for baseline, use scheduled agent evidence to prevent the next operational ticket. | This slide is about the stack position.<br/><br/>Native views show baseline cost management. The scheduled tasks add trust gates, anomaly context, fiscal context, capacity risk, and AI workload cost evidence.<br/><br/>That is the difference between a dashboard and an operating loop. | kind=ASK_C · addresses=48 verdict=green | None |
| P2.14.A | P2.14 Run-it-for-them ops model | #55 "Post-deploy ops model — who runs this after we leave?" | Ask | “Who runs this after we leave?”<br/>Post-deploy operations model | Customers ask who runs this after the consultant leaves.<br/><br/>The schedule does. Agents review, humans decide. Recurring analysis stays cheap. Decisions stay with the right owner.<br/><br/>That is the operating leverage finance is funding. | kind=ASK_A · addresses=55 verdict=green | None |
| P2.14.B | P2.14 Run-it-for-them ops model | #55 "Post-deploy ops model — who runs this after we leave?" | Implementation | • The schedule carries the recurring analysis rhythm; humans still own decisions, approvals, governance, purchases, and production-impacting changes.<br/>• The SRE Agent README lists five specialist subagents, three skills, thirty-three tools, eighteen scheduled tasks, and a FinOps Hub Kusto connector.<br/>• Outlook and Teams notification connectors are optional post-deploy setup, not default `azd up` output.<br/>• Read the operating model as scheduled analysis plus human review, not autonomous business decisions.<br/>• This is how the release prevents tickets without pretending to own every downstream action. | The operating leverage is the schedule.<br/><br/>The README gives the concrete package: five subagents, three skills, thirty-three tools, eighteen scheduled tasks, and the hub connector. Humans still own decisions and production-affecting changes.<br/><br/>That split is the support model. | kind=ASK_B · addresses=55 verdict=green | None |
| P2.14.C | P2.14 Run-it-for-them ops model | #55 "Post-deploy ops model — who runs this after we leave?" | Details | • References:<br/>```<br/>src/templates/sre-agent/README.md<br/>src/templates/sre-agent/CATALOG.md<br/>src/templates/sre-agent/sre-config/scheduled-tasks/<br/>```<br/>• Operating pattern:<br/>```<br/>schedule runs evidence task<br/>agent produces report or card through configured delivery path<br/>human owner reviews exception<br/>approved owner fixes the issue<br/>next run verifies whether risk is gone<br/>```<br/>• Setup pattern: configure optional notification connectors after deployment, choose a small first task set, and expand only after signal quality is proven. | This is the post-deploy model.<br/><br/>Task runs. Evidence lands. Owner reviews. Owner fixes. Next run verifies. The agent does not become the finance, governance, or platform owner.<br/><br/>That is how it keeps work alive after the engagement. | kind=ASK_C · addresses=55 verdict=green | None |
| P2.15.A | FTK supportability — HONEST | #49 "Is FTK Microsoft-supported? Who do I call?" · #50 "Positioning safety — will Microsoft pull this in 12 months?" · #51 "Support escalation path — CSS punted, GitHub only" | Ask | &gt; "Is FTK Microsoft-supported?"<br/>&gt; "Will Microsoft pull this in 12 months?"<br/>&gt; "When CSS punts, who owns the escalation?" | Is the toolkit Microsoft-supported. Will Microsoft pull this in twelve months. When CSS redirects the case, who owns it.<br/><br/>These are deal-risk questions. Naming the support model honestly protects the relationship.<br/><br/>The honest answer is the slide. | kind=ASK_A · addresses=49,50,51 verdict=red | None |
| P2.15.B | FTK supportability — HONEST | #49 "Is FTK Microsoft-supported? Who do I call?" · #50 "Positioning safety — will Microsoft pull this in 12 months?" · #51 "Support escalation path — CSS punted, GitHub only" | Implementation | • The repo README states the FinOps Toolkit is licensed under the MIT license.<br/>• The support posture for this deck is honest OSS plus GitHub workflow, with agent diagnostics such as `hubs-health-check` to tighten repro quality.<br/>• Do not imply CSS case ownership, phone support, or a product SLA for the toolkit itself.<br/>• Platform issues still go to the owning Azure service team with the same evidence package.<br/>• Credibility comes from saying this before the customer opens the wrong ticket. | Open-source license, GitHub workflow, and diagnostic evidence.<br/><br/>The SRE Agent improves repro quality, but it does not create a product support contract for the toolkit. Platform defects go to the owning Azure service with the evidence attached.<br/><br/>Grounding: root `README.md` license section and SRE Agent health-check task. | kind=ASK_B · addresses=49,50,51 verdict=red | None |
| P2.15.C | FTK supportability — HONEST | #49 "Is FTK Microsoft-supported? Who do I call?" · #50 "Positioning safety — will Microsoft pull this in 12 months?" · #51 "Support escalation path — CSS punted, GitHub only" | Details | • What it is:<br/>```<br/>MIT-licensed open-source toolkit<br/>GitHub workflow for toolkit issues<br/>scheduled-task diagnostics for reproducible evidence<br/>field or partner sponsorship when a customer program needs coordination<br/>```<br/>• What it is not:<br/>```<br/>CSS ownership for FTK<br/>product SLA for the toolkit<br/>phone support contract<br/>promise that every toolkit issue is an Azure product bug<br/>```<br/>• Evidence package: run `hubs-health-check`, capture failing task output, include version and freshness diagnostics, then open the GitHub issue or send the platform defect to the owning service team. | The details protect the relationship.<br/><br/>Say what it is and what it is not. Use the agent to make the issue better documented, not to invent a support contract.<br/><br/>Diagnostics before escalation. | kind=ASK_C · addresses=49,50,51 verdict=red | None |
| 2.99 | End of Part 2 | — | Operating motion / what comes next | • The run-the-shop answer is scheduled tasks, verified tools, optional configured delivery, and honest owner boundaries.<br/>• First evidence tasks: `hubs-health-check`, `mom-report`, `ytd-report`, `capacity-daily-monitor`, and `ai-workload-cost-analysis`.<br/>• Honest owner paths: Azure AI Foundry PG, CSS/SGS/UAT, Commerce and Cost Management, FTK OSS and GitHub, FinOps CoE, government, and customer governance owners.<br/>• Next: pick three live tasks, configure the delivery path if needed, and turn customer review into an operating cadence. | That is the operating motion.<br/><br/>Scheduled tasks. Verified tools. Configured delivery where available. Honest owner boundaries.<br/><br/>Pick a small first task set and run it against a real customer hub. | kind=BULLETS | None |

### Table 3 — Honest cluster + close (~8 slides)

| # | Cluster | Asks (verbatim) | Title | Content | Speaker notes | Layout | Screenshots / assets |
|---|---|---|---|---|---|---|---|
| H.1 | Honest | #3 "AOAI PTU throttling confusion (TPM still applies)" · #4 "AOAI model availability by region" · #5 "AOAI production capacity GA expectations" · #6 "AOAI TPM/RPM tier behavior" | AOAI platform gaps — Azure AI Foundry PG | • Ask `#3`: AOAI throttling and runtime behavior stay with Azure AI Foundry PG.<br/>• Ask `#4`: region and capacity-side AOAI platform gaps stay with Azure AI Foundry PG.<br/>• Ask `#5`: model lifecycle and GA timing stay with Azure AI Foundry PG.<br/>• Ask `#6`: AOAI service-side platform controls stay with Azure AI Foundry PG.<br/>• Use FTK cost, token, and capacity evidence as the input package; do not position FTK as the platform owner. | Throttling, runtime behavior, regional model availability, GA timing, and service controls live with the Azure AI Foundry product group.<br/><br/>The toolkit can carry cost, usage, and resource evidence into that conversation.<br/><br/>It is not the platform owner. State the boundary out loud. | kind=BULLETS | None |
| H.2 | Honest | #15 "SGS capacity-blocked milestones (MACC risk)" · #16 "UAT / quota escalation process (SAP selection, delays)" · #22 "Zonal HA subscription topology" | Capacity process gaps — CSS, SGS, UAT | • Ask `#15`: SGS capacity-blocked milestones stay with CSS, SGS process, and Azure capacity supply teams.<br/>• Ask `#16`: escalation-path and approval-process friction stay with CSS, SGS, and UAT process owners.<br/>• Ask `#22`: procurement and request-process latency stay with UAT process and Azure capacity teams.<br/>• Use agent evidence for demand, quota, SKU, CRG, and impact, then send the process ask to the owning path. | Milestone friction, escalation paths, and procurement latency live with CSS, SGS, UAT, and the Azure capacity teams.<br/><br/>The agent supplies demand, quota, SKU, CRG, and impact evidence.<br/><br/>It does not shorten the approval path. Evidence first, owner second. | kind=BULLETS | None |
| H.3 | Honest | #39 "Multi-cloud cost normalization (AWS / GCP / Oracle)" · #40 "MCA transition disrupting allocation" · #31 "GitHub Copilot UBB pricing shock" | Multi-cloud, MCA, allocation — Commerce + Cost Management | • Ask `#39`: multi-cloud cost normalization stays with the broader commerce and management platform stack, not this FTK release.<br/>• Ask `#40`: commerce and billing-model gaps across MCA constructs stay with Commerce and Cost Management owners.<br/>• Ask `#31`: allocation platform gaps beyond current Azure tag and hierarchy evidence stay with Cost Management platform owners.<br/>• Use FTK for Azure evidence, then send cross-cloud and commerce-model asks to the platform owner. | Multi-cloud normalization, MCA platform semantics, GitHub Copilot usage-based billing, and broader allocation features live with Commerce and the Cost Management platform team.<br/><br/>Use this release for the Azure side.<br/><br/>Cross-cloud and commerce-model questions belong with the platform owner. | kind=BULLETS | None |
| H.4 | Honest | #49 "Is FTK Microsoft-supported? Who do I call?" · #50 "Positioning safety — will Microsoft pull this in 12 months?" · #51 "Support escalation path — CSS punted, GitHub only" · #44 "FinOps delivery capacity gap (FTK is unsupported)" | FTK supportability — OSS community + GitHub | • Ask `#49`: supportability question stays with the FTK OSS community and GitHub workflow.<br/>• Ask `#50`: product-lifecycle anxiety needs clear OSS governance and roadmap communication, not a CSS promise.<br/>• Ask `#51`: escalation-path confusion needs honest routing and issue hygiene.<br/>• Ask `#44` and `#54`: program and community-operating questions stay with the FTK OSS community plus GitHub workflow, not a product SLA.<br/>• Standardize diagnostics, issue templates, and owner routing. | Supportability questions, lifecycle anxiety, escalation confusion, community process, and feedback loops belong to the FinOps Toolkit OSS community and the GitHub workflow.<br/><br/>The agent improves diagnostic quality.<br/><br/>It does not create a CSS contract. | kind=BULLETS | None |
| H.5 | Honest | #42 "FinOps staffing ambiguity (who owns at customer?)" · #67 "L100 / L300 enablement content for CSAs" · #72 "Gov / DoD support boundary clarity" · #65 "MACC visibility in Hub" | Enablement and governance — FinOps CoE + field | • Ask `#42`: practice enablement gaps stay with the FinOps CoE and field enablement owners.<br/>• Ask `#67`: maturity coaching and field readiness stay with the FinOps CoE and enablement leads.<br/>• Ask `#72` and `#73`: government and governance-specific policy asks stay with Gov and policy owners.<br/>• Ask `#65`: operating-governance accountability stays with the customer governance team and field program owners.<br/>• Use toolkit evidence in the meeting, then send policy and enablement asks to the right program owner. | Practice enablement, maturity coaching, government policy, and operating-governance accountability sit with the FinOps CoE, field enablement leads, government and policy owners, and the customer's own governance team.<br/><br/>The toolkit brings evidence into those rooms.<br/><br/>It does not replace those owners. | kind=BULLETS | None |
| Z.1 | Close | — | The first customer run | Pick one customer hub and invoke one high-value scheduled task such as `capacity-daily-monitor`, `hubs-health-check`, or `mom-report`.<br/>If Teams delivery is configured, confirm the connector and delivery path before treating the card as the operating artifact.<br/>Read the output with the customer and agree on the first exception to fix.<br/>Book a finance review and walk the customer's own YTD or month-over-month output. | Pick one customer hub.<br/><br/>Run one high-value task — capacity-daily-monitor, hubs-health-check, or mom-report. Confirm the configured delivery path if Teams is part of the rollout. Read the output with the customer. Agree on the first exception to fix.<br/><br/>Then book the finance review and walk the customer's actual numbers. | kind=BULLETS | None |
| Z.2 | Close | — | Resources and next steps | GitHub repo: `https://github.com/microsoft/finops-toolkit/tree/dev/src/templates/sre-agent`<br/>Task catalog: `https://github.com/microsoft/finops-toolkit/blob/dev/src/templates/sre-agent/CATALOG.md`<br/>Deployment guide: `https://github.com/microsoft/finops-toolkit/blob/dev/src/templates/sre-agent/README.md`<br/>Community Teams channel: use the release enablement Teams space for follow-up threads<br/>Session recording: use the training post recording link for replay and customer prep | Everything is in the GitHub repo.<br/><br/>The catalog, the deployment guide, the source code. Use the release enablement Teams space for follow-up threads.<br/><br/>The session recording is available for replay and customer prep. | kind=BULLETS | None |
| Z.3 | Close | — | Thank you / Q&A | Thank you.<br/>Feedback form: QR code or short link on the final render.<br/>Speaker contact: release enablement alias and follow-up Teams thread. | Thanks for watching.<br/><br/>Use the feedback form on screen.<br/><br/>The release enablement alias is the fastest way to follow up. | kind=TITLE | None |

---

## Cluster index — what each SME drafter owns

| Cluster | Owner SME | Asks | Verdict | Evidence pack |
|---|---|---|---|---|
| P1.1 Quota ≠ Capacity | capacity-manager | 8,13,14,23,24 | 🟢 | capacity-daily-monitor.md + capacity-health-overview.svg |
| P1.2 CRG mechanics + idle billing waste | capacity-manager | 9,12,19 | 🟢 | capacity-monthly-planning.md + chart3-governance-scorecard.svg |
| P1.3 RI vs SP vs ODCR + sequencing | finops-practitioner + capacity-manager | 17,18 | 🟡 | benefit-recommendation-review.md + benefit-recommendation-charts.svg |
| P1.4 SGS capacity-blocked milestones | capacity-manager | 15 | 🟢 | capacity-quarterly-strategy.md + quarterly-commitment-alignment.svg |
| P1.5 Customer refuses to change region | capacity-manager | 20 | 🟡 | sku-availability-audit.md + adx-sku-availability.svg |
| P1.6 AOAI quota + GPU + token forecasting | capacity-manager + finops-practitioner | 1,2,7 | 🟡 | ai-workload-cost-analysis.md + ai-daily-trend.svg |
| P1.7 AOAI throttling/region/GA — HONEST | capacity-manager | 3,4,5,6 | 🔴 | none — out of scope by design |
| P1.8 MACC discount verification | cfo + finops-practitioner | 35 | 🟢 | cost-optimization.md + chart-commitment-savings.svg |
| P1.9 Vendor displacement | cfo | 47 | 🟢 | finops-sre-agent-strategic-analysis-and-industry-positioning-april-2026 memory note |
| P1.10 Sovereign / Gov cloud | capacity-manager | 60,72 | 🟡/🔴 | mixed |
| P1.11 EA → MCA migration safety | finops-practitioner | 52 | 🟡 | hubs-health-check.md + monitoring-scope-validation.md |
| P2.1 Capacity rhythm | capacity-manager | 21,24 | 🟢 | capacity-weekly-supply-review.md + chart4-commitment-monthly.svg |
| P2.2 AOAI token attribution | finops-practitioner + cfo | 25,26,28 | 🟢 | ai-workload-cost-analysis.md + ai-daily-trend.svg + ai-model-comparison.svg + ai-mom-comparison.svg |
| P2.3 AI budget brake pedal | finops-practitioner | 27 | 🟡 | budget-coverage-audit.md + budget-alert-config.svg |
| P2.4 AI model substitution + prompt economics | finops-practitioner | 29,30 | 🟢/🟡 | ai-workload-cost-analysis.md + ai-model-comparison.svg |
| P2.5 Daily cost-mover briefing | finops-practitioner | 36,37 | 🟢 | mom-report.md + chart-monthly-cost-trend.svg |
| P2.6 Anomaly detection chain | finops-practitioner | 41 | 🟢 | alert-coverage-audit.md + alert-coverage-matrix.svg |
| P2.7 CFO reconciliation trust gap | cfo | 32,33,34 | 🟡 | ytd-report.md + hubs-health-check.md |
| P2.8 Hub freshness verdicts | finops-practitioner | 38,58,66 | 🟢 | hubs-health-check.md + monitoring-scope-validation.md |
| P2.9 RI/SP math + portal reconciliation | finops-practitioner | 62,63 | 🟢/🟡 | benefit-recommendation-review.md + cost-optimization.md |
| P2.10 Allocation hygiene + post-M&A | finops-practitioner | 36,38 | 🟡 | budget-coverage-audit.md + tag-coverage-audit.svg |
| P2.11 FinOps Framework operationalization | finops-practitioner | 43,45 | 🟢 | CATALOG.md + capacity-quarterly-strategy.md + chart3-governance-scorecard.svg |
| P2.12 Dashboard adoption → Teams cadence | finops-practitioner | 70 | 🟢 | capacity-daily-monitor.md + capacity-weekly-supply-review.md (Teams card examples) |
| P2.13 Native CM gaps (what we exceed) | finops-practitioner | 48 | 🟢 | alert-coverage-audit.md + advisor-suppression-review.md |
| P2.14 Run-it-for-them ops model | cfo | 55 | 🟢 | aggregate of all 18 task evidence files (the schedule IS the answer) |
| P2.15 FTK supportability — HONEST | finops-practitioner | 49,50,51 | 🔴 | none — out of scope by design |

---

## Voice and presentation rules (carry from V6 — non-negotiable)

1. **Sentence case** for every title, header, table column, bullet.
2. **Second person.** "You" addresses the field reader. "Your customer" addresses the deployment owner.
3. **No first person.** No "I". No "we" except sparingly when speaking as Microsoft.
4. **Verbatim asks may name accounts** (Snowflake INT, Aon, Synopsys, Trimble) since they're public internal corpus citations from the canonical memory note. **Body slide content does NOT name customers.**
5. **No outcome verbs.** Banned: surfaces, transforms, empowers, unleashes, retires, eliminates. Use named tools, named tasks, named subagents, named runtimes, named API surfaces.
6. **Active voice, present tense.**
7. **Honest scope on write ops.** v1 has 6 write-capable tools (`deploy-budget`, `deploy-bulk-budgets`, `deploy-anomaly-alert`, `deploy-bulk-anomaly-alerts`, `suppress-advisor-recommendations`, `resource-graph-query`) — name them where relevant; don't hide them, don't over-claim.
8. **Speaker notes target 60-90 seconds each.**
9. **Slide A = ASK** (verbatim customer quote, low density, big type — speaker reads aloud and pauses). **Slide B = SHOW** (the answer with chart/Teams-card eye candy + named tools — proof carries the slide). **Slide C = TELL** (L400 details — YAML, cron, KQL, runtime, Monday-move command; code carries the slide).
10. **Honest scope** is a credibility weapon, not a weakness. The 25 🔴 asks in the canonical table get one cluster of slides naming the actual owner per ask group.
11. **Every body slide has an `addresses=N,N verdict=green|yellow|red` directive in its layout cell** AND **a citation block at the end of speaker notes** referencing the canonical memory URI + ask number(s) + evidence pack path.

---

## Build mechanics

New layout kinds the build script must support:

| Kind | Renderer behavior | Mnemonic |
|---|---|---|
| `ASK_A` | Big-type indented blockquote (the verbatim ask) + small attribution line below + ask-coverage footer chip (auto-rendered from `addresses=N verdict=X`) | **ASK** |
| `ASK_B` | Title + 1-paragraph answer text (left ~40%) + chart/Teams-card image (right ~60%) + footer chip | **SHOW** |
| `ASK_C` | Title + monospace code block (YAML / cron / KQL / Monday-move) + footer chip | **TELL** |

Existing kinds carry from V7 unchanged.

Per-row directive `addresses=N,N verdict=green|yellow|red` in the layout cell is parsed by the build pipeline; renderer emits a footer band at the bottom of every body slide showing the ask numbers + colored verdict pill.

---

Related concepts: [[FinOps Toolkit]] · [[FinOps toolkit SRE Agent]] · [[Azure SRE Agent]] · [[FinOps Hub]] · [[MCAPS]] · [[azure-capacity-manager]] · [[chief-financial-officer]] · [[finops-practitioner]] · [[ftk-database-query]] · [[ftk-hubs-agent]] · [[capacity-reservation-groups]] · [[quota-groups]] · [[AOAI]] · [[FOCUS]] · [[KQL]] · [[CloudHealth]] · [[Cloudability]] · [[Vantage]] · [[Spot.io]] · [[Apptio]] · [[positioning-synthesis]] · [[finops-sre-agent-strategic-analysis-and-industry-positioning-april-2026]] · [[mcaps-internal-asks-finops-toolkit-azure-capacity-ai-cost-may-2026]]
