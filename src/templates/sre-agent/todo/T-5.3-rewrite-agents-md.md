# T-5.3 Rewrite AGENTS.md with complete template reference

**Pipeline**: lightweight
**Pipeline Digest**: a]lightweight
**Priority**: P1
**Parent**: session-1777654291702-12465

## Problem

`AGENTS.md` is 16 lines with three bugs:
1. Malformed YAML: `:default: finops-hubenvironments:` is a concatenation of `:default: finops-hub` and `environments:` (missing newline)
2. References `test-driven-development` skill which doesn't exist in `sre-config/skills/`
3. Doesn't mention the 5 subagents, 3 skills, 33 tools, 18 scheduled tasks, or Kusto connector

## Fix

Rewrite `AGENTS.md` as a comprehensive agent reference for the SRE agent template:

1. Fix the YAML block — proper newlines, correct structure
2. Remove `test-driven-development` skill reference, replace with actual skills: `azure-capacity-management`, `azure-cost-management`, `finops-toolkit`
3. Add sections for: subagents, skills, tools, scheduled tasks, connector, deployment info
4. Keep placeholder tokens (`<subscription-id>`, etc.) since this is a template
5. Reference README.md for deployment details rather than duplicating

Source: `sre-config/agents/*.yaml` for subagent names, `sre-config/skills/*/` for skill names, `README.md` for component counts.

## Acceptance

- YAML block parses correctly
- No reference to nonexistent skills
- Documents all 5 subagents by name
- Documents all 3 skills by name
- Mentions tool count and scheduled task count
- Under 80 lines (reference doc, not a novel)

## Pending

- Stage ID: pending
- Record Kind: outcome
- Agent: pm
- Recorded At: 2026-05-01T21:46:02.510577+00:00
- Outcome: PASS
- Notes: P1 doc fix — dispatching SWE to rewrite AGENTS.md
