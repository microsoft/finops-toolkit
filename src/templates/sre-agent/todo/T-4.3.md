---
id: T-4.3
title: Deploy and verify all scheduled tasks on live agent
pipeline: gsd
depends_on: [T-4.1, T-4.2]
---

# T-4.3 — Deploy and verify all scheduled tasks on live agent

**Component**: Live agent `sre-agent-dl4veickx4bpg`

## Scope

Redeploy all 18 scheduled tasks (9 updated + 9 new) to the live agent via `srectl scheduledtask apply`. Trigger at least one execution of each and verify they complete without tool-not-found errors and produce meaningful output.

## Acceptance criteria

1. All 18 tasks deployed via `srectl scheduledtask apply` without errors
2. At least 3 tasks manually triggered and confirmed producing output (Teams message or #remember)
3. No tasks reference removed tools (zone-mapping, spot-placement-scores)
4. Tasks using Python tools correctly pass `client_id` and get real data (not 403/empty)
5. Capacity tasks (CapacityDailyMonitor, NonComputeQuotaAudit) use Python tools instead of falling back to `az` CLI
