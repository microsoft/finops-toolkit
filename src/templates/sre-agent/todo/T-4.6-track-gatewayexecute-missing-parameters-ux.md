---
id: "T-4.6"
title: "Track GatewayExecute missing parameters UX"
pipeline: "lightweight"
priority: "P4"
sprint: 4
---

## Problem
During evaluation of `src/templates/sre-agent`, multiple `maenifold-GatewayExecute` calls without a `parameters` object failed with `McpError: MCP error -32602: missing field `parameters``.

## Expected behavior
The MCP surface should either document/enforce the required envelope clearly at the tool schema level or provide a more actionable validation message that names the missing top-level field and shows the expected shape.

## Lineage
Parent sequential thinking session: `session-1777641861629-53016`.

Related concepts: [[MCP-tooling-error]], [[GatewayExecute]], [[sre-agent-template]].
