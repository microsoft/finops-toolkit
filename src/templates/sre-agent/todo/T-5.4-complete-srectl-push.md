# T-5.4 Complete srectl push to ftk-sre-agent deployment

**Pipeline**: lightweight
**Pipeline Digest**: a]lightweight
**Priority**: P1
**Parent**: session-1777654291702-12465

## Problem

The `ftk-sre-agent` deployment completed infrastructure (SRE Agent, identity, monitoring) and `srectl init` connected (0 agents found), but the full srectl config push (skills, agents, knowledge, connector) wasn't completed.

The post-provision hook failed on first run (CRLF bug) and the manual re-run only executed through init + RBAC. The `apply_skills`, `apply_agents`, `apply_tools`, `apply_knowledge`, and `apply_scheduled_tasks` phases need to run.

## Fix

Run `post-provision.sh` against the live agent endpoint to push:
- 3 skills (azure-capacity-management, azure-cost-management, finops-toolkit)
- 5 subagents (azure-capacity-manager, chief-financial-officer, finops-practitioner, ftk-database-query, ftk-hubs-agent)
- Knowledge docs from sre-config/knowledge/
- Kusto connector from sre-config/connectors/
- Tools from tools/ directory
- Scheduled tasks from sre-config/scheduled-tasks/ (if directory exists)

Agent endpoint: `https://sre-agent-dyisc724uziua--0fe87ad4.de5105f9.eastus2.azuresre.ai`
Hub cluster URI: `https://msbwftksretrey.westus.kusto.windows.net/hub`

## Acceptance

- `srectl agent list` shows 5 agents
- `srectl skill list` shows 3 skills
- Kusto connector configured for FinOps Hub

## Pending

- Stage ID: pending
- Record Kind: outcome
- Agent: pm
- Recorded At: 2026-05-01T21:46:02.528788+00:00
- Outcome: PASS
- Notes: P1 operational fix — dispatching SWE to complete srectl push to live agent
