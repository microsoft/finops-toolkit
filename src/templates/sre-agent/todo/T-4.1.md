---
id: T-4.1
title: Wire Python tools into existing scheduled tasks
pipeline: gsd
---

# T-4.1 — Wire Python tools into existing scheduled tasks

**Component**: `sre-config/scheduled-tasks/*.yaml` (all 9 existing tasks)

## Scope

Update all 9 existing scheduled task prompts to reference the 12 deployed Python tools by name and instruct the agent to pass `client_id=d748a310-d522-477d-8e5f-6914f4152f31` (or the UAMI client ID from deployment) when invoking them.

## Mapping

| Scheduled task | Python tools to wire in |
|---|---|
| CapacityDailyMonitor | vm-quota-usage, non-compute-quotas, capacity-reservation-groups, sku-availability, data-freshness-check |
| CapacityWeeklySupplyReview | vm-quota-usage, non-compute-quotas, capacity-reservation-groups, sku-availability, benefit-recommendations |
| CapacityMonthlyPlanning | vm-quota-usage, capacity-reservation-groups, benefit-recommendations, resource-graph-query |
| CapacityQuarterlyStrategy | benefit-recommendations, resource-graph-query, vm-quota-usage |
| CostOptimization | resource-graph-query, non-compute-quotas, benefit-recommendations |
| AIWorkloadCostAnalysis | resource-graph-query, vm-quota-usage |
| HubsHealthCheck | data-freshness-check |
| MOM | data-freshness-check, vm-quota-usage |
| YTD | benefit-recommendations, resource-graph-query |

## Acceptance criteria

1. Each scheduled task prompt explicitly names the Python tools it should use
2. Each prompt includes the `client_id` parameter instruction for UAMI auth
3. No scheduled task references removed tools (zone-mapping, spot-placement-scores)
4. All 29 repo tests still pass after changes
5. Redeployed tasks execute on the live agent without tool-not-found errors
