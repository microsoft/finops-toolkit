// YTD Total Effective and Billed Cost
// Returns the total effective and billed cost from the start of the current year to date.
// Parameters: none (uses current year)
// Author: FinOps Toolkit Team
// Last Tested: 2025-05-16
//
let startOfYear = startofyear(now());
Costs()
| where ChargePeriodStart >= startOfYear
| summarize TotalEffectiveCost = sum(EffectiveCost), TotalBilledCost = sum(BilledCost)
