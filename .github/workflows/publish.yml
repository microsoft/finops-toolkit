on: workflow_dispatch            

jobs:
    publish:
        runs-on: windows-latest
        steps:
            - uses: actions/checkout@v3
            - name: Test
              shell: pwsh
              run: npm