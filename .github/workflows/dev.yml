name: 'Run Pester Tests'

on: [push]
jobs:
    run_pester_tests:
        name: Run Unit Tests
        runs-on: Windows-latest
        steps:
        - uses: actions/checkout@v3
        - name: Test with Pester
          shell: pwsh
          run: ./src/scripts/Start-PesterTests.ps1
            