id: issues-03-needs-info
name: GitOps.PullRequestIssueManagement
description: Handles when an issue needs information
owner:
resource: repository
disabled: false
where:

# spell-checker:disable
configuration:
  resourceManagementConfiguration:
    eventResponderTasks:
      - description: >-
          When an issue has been triaged with `#needs-info` in a comment:
          - Assign to author
          - Remove the `Needs: Triage 🔍` label
          - Label with `Needs: Information`
        if:
          - payloadType: Issue_Comment
          - isOpen
          - commentContains:
              pattern: '#needs-info'
          - or:
              - activitySenderHasPermission:
                  permission: Admin
              - activitySenderHasPermission:
                  permission: Write
        then:
          - addLabel:
              label: 'Needs: Information'
          - removeLabel:
              label: 'Needs: Triage 🔍'
          - assignTo:
              author: True
      - description: >-
          When an issue that needs info has been responded to:
          - Remove author assignee
          - Remove the `Needs: Information` label
          - Label with `Needs: Triage 🔍`
        if:
          - payloadType: Issue_Comment
          - isOpen
          - isActivitySender:
              issueAuthor: true
        then:
          - removeLabel:
              label: 'Needs: Information'
          - addLabel:
              label: 'Needs: Triage 🔍'
          - assignTo:
              author: False
onFailure:
onSuccess:
